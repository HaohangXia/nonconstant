# 03 · LEDGER — 跨轮账本

> **这是 loop 的脊柱。**agent 会忘,仓库不会忘。
> 每轮结束必须追加。**只增不改**(除非修正笔误)。
> 下一轮开始前必须先读本文件 —— 准入门槛 `G5` 靠它防重复提案。

---

## 状态

| 项 | 值 |
|---|---|
| 已完成轮次 | 1 |
| 累计提案数 | 4(另丢弃 6) |
| 累计采纳数 | 2 |
| 连续低采纳轮次 | 0 |
| 收敛状态 | **CONTINUE** |

---

## 轮次记录

### ROUND 1

- **日期**:2026-08-21
- **Maker 环境**:`nonconstant-maker` sub-agent,全新隔离 session,只读工具 + Bash 查询;可直接读 `vendor/spec-kit/`
- **Challenger 环境**:`nonconstant-challenger` sub-agent,独立 session,只收到剥离后的「变更内容 + 证据」清单;污染检查报 CLEAN
- **本轮推导**:定理 6 条(与 PLAN 一致 4 / 新增 2 / 无法推出 0)。新增两条(E「被否决选项须外部登记」、F「产物存在规模上限,超限后验证只被假装执行」)由 Maker 主动丢弃 —— 其机制已由本 LEDGER 否决库、P5 `explicitly_out_of_scope`、§7 400 行上限落地

| ID | 类别 | 锚点 | 变更摘要 | Ben | Cost | 修正 | Net | 结果 |
|----|------|------|---------|-----|------|------|-----|------|
| M1 | FIX/MODIFY | §2.2 依赖链 + 「待解」 | 把「extensions 删除待解」替换为 A(KEEP)/B(内联 15 行 `normalize_priority` 后删)/C(抽中立模块)三选项表,并记录依赖面尺寸 | +4 | -2 | -2 | **0** | 否决 |
| M2 | FIX/MODIFY | §2.2 耦合方向 + 处置表 | 补一条支配边:`specify_cli/__init__.py` 模块级 eager 注册全部子系统,删任一项须同步摘除其 2 行注册块;处置表「删」改「删 + 摘除根注册块」 | +4 | -1 | 0 | **+3** | **采纳** |
| M3 | DELETE | §6 Q1;§2.2 `events.py` 行 | 删 Q1 整行;`events.py` 处置由「待查」改为已判定结论 | +3 | -1 | +1 | **+3** | **采纳** |
| M4 | MODIFY | §5 P3 | P3 示例由独立 `workflow.yml` 改为 workflow overlay 形式;§2.2 记 overlays 为 nonconstant 功能依赖 | +3 | -3 | -2 | **-2** | 否决 |

**采纳项写入说明**(O3 相关):
- M3 的替换文案按 Challenger 的**通过条件**逐字写入(「`events.py` 与 `workflows/` 零耦合;但 `integrations/base.py:33` 顶层导入它、`integrations/` 为 KEEP ⇒ `events.py` KEEP」)。Maker 原文案「其存废由根注册边决定」经 Challenger 独立复核为**不完整**:`integrations/base.py:33` 是顶层绝对导入 `from ..events import ...`,而 `integrations/` 处置为 KEEP。
- M2 变更内容中「删 `extensions/` 须连带删 `presets/`」一句**未写入 PLAN**:该句以 M1 选项 B 为前提,而 M1 已否决,写入即悬空引用。其底层事实(`presets/__init__.py:39` 顶层 `from ..extensions import ...`,Challenger 独立复现)记于此备查。

**丢弃提案**(未过准入门槛):

| 摘要 | 丢弃门槛 | 原因 |
|------|---------|------|
| 新增定理 E「被否决选项必须外部登记」 | G6 | 机制已由 03-LEDGER 否决库 + P5 `explicitly_out_of_scope` 实现,写成定理只增行数 |
| 新增定理 F「产物存在规模上限」 | G6 | 已由 §7 复杂度预算与 400 行硬上限实现 |
| 「§1 workflow.yml 数据需修正」 | G2 | 复核全部为真:`workflow.yml` 78 行;step = specify / review-spec(gate) / plan / review-plan(gate) / tasks / implement = 4 command + 2 gate + 0 shell;全仓唯一 workflow yml |
| 「§2.1 行数需复核」 | G2 | 逐条实测为真:`engine.py` 1737、`steps/shell/__init__.py` 169、`steps/gate/__init__.py` 340、`workflows/` 24 个 .py 共 11,691 |
| 「§2.2 `extensions/` 8,020 行需复核」 | G2 | 实测 `__init__.py` 5,316 + `_commands.py` 2,704 = 8,020,与文档一致 |
| 「建议删 `integrations/` 至 4–5 个以减重」 | G5 + 举证责任 | 属 Q5 已挂起未决项;38 个 integration 无优先级标记可推断,举证不足 → KEEP |

**ROUND-1-VERDICT**:CONTINUE

**交接提示**(≤50 字,仅指方向):§2.2 矛盾仍在。查一行 KEEP 能否消解,勿再列选项表。

---

### ROUND 2

_(同上格式)_

---

## 否决库

> `Net ≤ 0` 的提案进此表。**后轮不得无新证据重提。**
> 重提时必须在"新证据"栏写明,否则按 `G5` 丢弃。

| 提案摘要 | 首次轮次 | Net | 最强反对理由 | 新证据(重提时填) |
|---------|---------|-----|------------|-----------------|
| M1 §2.2 三选项表(KEEP / 内联后删 / 抽中立模块) | 1 | 0 | 一行 KEEP 结论即可消矛盾,不必留 B/C 死表;选项表本身构成二阶复杂度(矛盾 → 选项表 → 选项间依赖注记),且 B 的举证责任转嫁给 M2,形成 M1↔M2 相互引用 |  |
| M4 §5 P3 示例改用 workflow overlay | 1 | -2 | overlay 写错 `extends` / `enabled` / 路径任一项 → `workflow run speckit` 照常绿、退出码 0、闸门从未存在;把失效模式从大声报错改为静默缺席,直接侵蚀 A1。且其负面断言「overlays 无 step 类型白名单」被 `engine.py:132-140 _get_valid_step_types()` 反证,证据不成立。所解决的上游覆盖问题在 pin 到 d1f50fc 后当下不存在 |  |

---

## 备查库

> `Net = +1~2` 的提案。有价值但未达阈值,后轮可凭新证据或与其他提案合并后重提。

| 提案摘要 | 轮次 | Net | 差在哪 |
|---------|------|-----|--------|
|         |      |     |        |

---

## 原意图重建档案

> 对 `[SK]` 组件做的 Chesterton's Fence 四问调查结果。
> **这是本 loop 最持久的资产** —— 即使方案推翻重来,这些调查仍然有效。

| 组件 ID | 它防的是什么 | 证据(可核查) | 我们场景下是否存在 | 错了会怎样 | 结论 |
|--------|------------|--------------|------------------|-----------|------|
| `SK-B6` |            |              |                  |           |      |
| `SK-B7` |            |              |                  |           |      |
| `SK-B8` |            |              |                  |           |      |
| `SK-overlays`<br>`workflows/overlays/`<br>1,466 行 / 6 文件<br>(R1) | 防本地定制在上游升级(`specify workflow add` / `specify bundle update`)时被覆盖丢失;次要防 overlay id 被用作安装路径段时的路径穿越 | ① `docs/reference/workflows.md:120` 逐字陈述所防问题 ② `overlays/schema.py:12-14` 三个保留 id 常量 + 「no path separators, no traversal, no dots」注释 ③ `tests/workflows/test_overlay_security.py` 独立存在(同族另有 5 个 overlay 测试文件)④ `layer_sources.py:50/53/138/213` `_ensure_contained_dir` ⑤ CHANGELOG `:62`(#3881 非字符串 operation 防护)、`:331`(#3662 多个 `insert_after` 组内顺序)。**证据等级:强(5 处独立可核查,Challenger 逐条复现)** | **存在,且更强**。P1/P3/P4 即"往 speckit workflow 注入 shell 闸门",与 overlay 目标用例同形;pin 后若跟进上游,升级覆盖问题原样存在 | 误删/误改 → `workflow run <id>` 无法解析已安装 workflow(`engine.py:886` 是唯一 by-ID 解析路径)→ **闸门整体不执行,且可能表现为"没有闸门被触发"而非报错**。可逆(vendored) | **KEEP**(四问全答,答案一致指向保留)。注:`overlays/schema.py:144` 每解析一个 overlay 即调用 `normalize_priority` —— 任何"把 overlay 提升为 nonconstant 功能依赖"的方案都会永久钉死 `extensions/` 依赖 |
| `SK-root-init`<br>`specify_cli/__init__.py`<br>`:509/516/521/559/566/573` 注册块<br>(R1) | 防 CLI 表面在代码搬家后断裂。注释自证:「Moved to extensions/_commands.py — **registered here to preserve CLI surface**」 | 5 处同构注释(`:507` `:514` `:557` `:564` `:571`)+ `# noqa: E402`(E402 = module level import not at top of file,只有**模块级**导入才触发,证明非函数体内延迟导入)+ `:576-585` re-export 段注释点名依赖方是 bundler 与 monkeypatch 测试。Challenger 独立复现导入行与紧随的 `_register_*(app)` 调用成对出现于 `:509/510` `:516/517` `:521/522` `:559/560` `:566/567` `:573/574` | **部分存在**。保留 `specify workflow ...` CLI 则 workflows 块必须留;被删子系统的对应块随之删除,此时"保持 CLI 表面"对已删子系统不再是需求 | 删块不删目录 → CLI 少子命令,行为可预期;删目录不删块 → `import specify_cli` 直接 ImportError,**进程启动即刻暴露**(最好的一类失效模式)。可逆 | **KEEP 默认态**。四问已答清,故"删目录 + 摘除对应 2 行块"属整块 DELETE 而非内部 MODIFY,举证责任可视为已尽 |
| `SK-integrations-eager`<br>`integrations/__init__.py:130`<br>`_register_builtins()`<br>(R1) | **重建不出。**可观察:38 个内建 integration 在包导入时全部 eager import 并注册;注释只说明命名约定(`key` 保连字符以匹配用户实际安装的 CLI 二进制名),**未说明为何必须 eager** | `:48-128` 字母序 import 块;`:130` 顶层调用;`:45-48` 注释仅涉命名。**无**关于 eager-vs-lazy 的注释、CHANGELOG 或 issue 线索 | **判定不了** —— 第 1 问未答上 | 未知。可能存在某处遍历全部已注册 integration,改 lazy 会让该遍历看到空表,**且不报错、只是少了条目**(静默失效) | **KEEP**。四问未全答 → 按举证责任铁律一律 KEEP。「我不知道它为什么 eager」不是裁剪的理由。§2.2「integrations 首发裁剪至 4–5 个」须注明:裁剪要改 `_register_builtins()` **函数体**,属 MODIFY 而非 DELETE,举证责任更重。与 Q5 联动 |
| `SK-events`<br>`events.py` 2,519 行<br>(R1) | 未做四问 | — | — | — | **KEEP 默认态,零举证**。本轮只判定「它不是 `workflows/` 的依赖」;其存废另受 `integrations/base.py:33` 顶层导入约束(`integrations/` = KEEP)。真要删须先另做四问 |
| `SK-v1.0.0-repin`<br>上游重 pin<br>(事实留存) | — | **spec-kit v1.0.0 发布于 2026-08-21**(`published_at 2026-08-21T11:40:31Z`),tag → commit `bca679051abb80d6cf0cd909f2539a28a10eb7eb`。**四条承重断言全部不变**,已逐条自查:① `steps/shell/__init__.py:70` `if proc.returncode != 0: return StepResult(status=StepStatus.FAILED…)`;② `steps/gate/__init__.py:45` `on_reject = config.get("on_reject", "abort")`;③ **官方 `workflows/speckit/workflow.yml` 与 v0.16.4 逐字节相同**(`md5 06fd89e478bb8e0e668917a5f02f9055`,`cmp` 无差异),78 行 / 4 command(未标 type,取默认)/ 2 gate / **0 shell**,且全仓唯一;④ `src/` grep `waiver\|amendment\|exemption\|expiry\|expires_at` 零命中,对照组 `templates/constitution-template.md:44` 命中,证明遍历生效。`steps/` 步骤类型 11 种无增减。CHANGELOG `[1.0.0]` 仅 `### Changed`,全为社区扩展/预设更新与 bugfix,无 breaking change | — | — | **nonconstant 的依赖面(shell step + gate step)零变化,方案无需重审。**代码增量:`workflows/` +381、`extensions/` +155、`presets/` +15、`engine.py` +51(1737→1788,致依赖链锚点 `engine.py:886` → `:929`,其余锚点未漂移) |
| `LATCH-incomplete-set`<br>§3 机制表替换指令<br>(2026-08-22,`e161e07`,事实留存) | — | nonconstant 侧下达 §3 机制表替换指令时,给出的 **8 行集合不完整** —— A2(会话切割)不在其中。照字面执行会**静默丢失**该条,且断掉 §1 LE 表与 D8 对它的引用。由执行方发现并保留,同时主动报告"超出字面指令"。⭐ 这是 **M1(判定者独立性)在文档层面的一次真实拦截**:下达方与执行方分离,执行方拦下了下达方的错误 | ⚠️ **同类错误 nonconstant 侧已发生多次** —— §8 #4~#8、#11~#14 多条同源:**给出一个不完整的集合,然后当作完整的使用** | — | 事实留存 <br>⭐ 现行操作规则见 `18-PROTOCOL.md` §1(R5) · §3(F3) |
| ⭐⭐ `LATCH-hook-three-legs`<br>PreToolUse hook 的三种失效<br>(2026-08-22,`e4ea6bb` 期间) | — | ⭐⭐ **同一机制、三种失效模式,现已凑齐:**<br>① sub-agent 写 `FORBIDDEN.txt`(15 号)⇒ ⭐ **该拦,拦住** = 正确<br>② `Bash` 通道(15 号)⇒ ⛔ **该拦,没拦** = 漏报,判据**过窄**<br>③ 只读 `git log && grep -n "…commit"` ⇒ ⛔ **不该拦,拦了** = ⭐ 误报,判据**过宽**<br>**根因**:`block-no-verify@1.1.2` 把 `grep -n "最后 commit"` 里的 `-n` + `commit` 误认成 `git commit -n`(`--no-verify` 的短写)。连带 `--amend` 也被拦 | **存在。** | ⭐⭐ **这是 D-12「判命令字符串不可判定」的正面实证。**此前 D-12 只有理论论证(拦 `>` ⇒ 绕 `python -c` ⇒ 无穷退化);现在有了**反方向**的经验证据 —— ⛔ 判据不只会漏,还会**误伤**。⇒ **两头都堵死:**放宽 ⇒ 误报阻断合法操作(③);收紧 ⇒ 漏报绕得过去(②)⇒ ⭐ **中间没有正确的点** | ⭐ **D-12 的「改判结果、不判命令」由建议升级为必须**(见 `16-DECISIONS.md` D-12)。已联动 `01-PLAN.md` §4 D2 副作用栏。<br>⭐ **附:遇误拦时绕开而不禁用** —— `--amend` 被拦后改用 `git reset --soft` + 重新提交,**钩子照常执行**,⛔ 全程未加 `--no-verify`、未禁用钩子。⇒ 这正是 §5 P2「不提供临时禁用开关」要的行为,现在有实证 |
| ⚠️⚠️ `LATCH-redcheck-confidence-limit`<br>基线自检引入之前,全部红检结论有此限制<br>(2026-08-23) | — | ⭐ **逐条数过,⛔ 非记忆**(遍历四个会话记录的 `tool_use` 输入):<br>· **直接 `bash .nonconstant/…` 跑判据:174 次**<br>· ⛔ **用 Python `subprocess` 调 bash 跑判据:3 次,全部在 2026-08-23 本轮,且全部失败**<br>⚠️ ⛔ 首次统计我把过滤条件写成「命令里含 `.nonconstant/`」⇒ **漏掉了 `python .probe2.py`**(Write 建的文件,命令里没有 `.nonconstant/`)。⭐ 自查后放宽为「输入里含 `subprocess.*(['\"]bash`」才数全,2 → 3。⇒ ⚠️ **统计方法本身也会有盲区**(`LATCH-count-mismatch-must-be-chased` 同族)。 | **存在。** | ⭐ **纯 bash 同样有风险,⛔ 不能因为「不是 subprocess」就免疫:**命令拼错 ⇒ `127`;路径不存在 ⇒ `1` 或 `2`;`sed`/`awk` 吃掉 Windows 路径反斜杠 ⇒ 参数变成垃圾路径 ⇒ 判据判 `2`(⚠️ 本项目已发生 **3 次**)。⇒ ⭐ **凡红检只写「期望非 0」,这些都会被记成通过。** | ⚠️⚠️ **如实标注,⛔ 不重跑、⛔ 不背书:**在基线自检(见 `LATCH-harness-failure-looks-like-red` 的方案)引入之前,**Phase 0~7 与 Phase 10 的全部红检结论,其可信度依赖于「探针跑起来了」这个未经验证的前提。**<br>⭐ **减轻因素(⛔ 不是免除)**:① 174/177 的判据调用是**直接 bash**,⛔ 未经 Python 转手;② 多数红检记录了**具体退出码**(`1` / `2` / `127`)而非「非 0」,⭐ 而工具失败最常见的 `127` 与判据的 `1` 可区分;③ 多数红检**同时记录了判据打印的原因串**(如「⛔ 已过期:until = Phase N」)—— ⭐ 那正是方案③要机器化的东西,当时是人眼在做。<br>⛔ **不减轻的部分**:期望值写作「非 0」且未记录原因串的那些条目,⚠️ **无法事后区分**。⇒ ⭐ 重验的正确时机是**下次触碰该判据时**,⛔ 不是现在批量重跑。 |
| ⭐⭐ `LATCH-engine-two-kinds`<br>「引擎」一直没定义,拆开后需求变清楚<br>(2026-08-23) | — | ⭐ **拆开看,DevLoop 与 spec-kit 提供的是两样不同的东西:**<br>· **执行引擎** = 能跑 shell · 能读退出码 · 失败能中止<br>· **生成引擎** = 把想法变成规格与任务清单<br>⇒ **DevLoop**:有编排(派单 / 并行 / 隔离工位),⛔ **无生成侧**;**spec-kit**:两者都有。<br>⇒ ⭐⭐ **nonconstant 只需要执行引擎。** | **存在。** | ⭐ 此前一直说「接编排层」「接引擎」,⛔ 而「引擎」从未被定义 ⇒ ⚠️ 于是「要不要自建 CLI」这类问题反复出现(§8 #5 撤回过一次)。⭐ 拆成两类后,答案是显然的:⛔ nonconstant 不需要生成侧,⭐ 它需要的只是「有人在固定时机跑我的脚本并看退出码」。 | ⭐⭐ **一般化:凡「我们需要 X」的判断,先问 X 是不是一个东西。**⛔ 把两种能力合称一个词,会让选型问题永远回不了答案。<br>⚠️ ⛔ **顺带订正一处措辞(⛔ 非订正一处错误)**:曾说 nonconstant「可挂在任何能跑 shell 的东西上」——⭐ 那句抄自 **Q14**,而 Q14 自己标着「未实测」。<br>⭐ **准确表述:nonconstant 的接口是退出码,理论上任何执行器都能调用,⛔ 但只在两处验证过。**<br>· ⭐ 已验证:`bash .nonconstant/<x>.sh` 直跑 · `.git/hooks/pre-commit`<br>· ⛔ 未验证:Makefile · GitHub Actions · spec-kit workflow<br>· ⚠️ 且有一处**已知移植障碍**:`upstream-pin` 是 `scope: bootstrap`,⛔ 只在 nonconstant 自己的仓里有意义。<br>⚠️ ⛔ **核实结果:仓内并不存在「可用于任何引擎」这类字面断言**(全仓 grep,⛔ 排除 vendor)——⭐ 本条是**预防性定义**,⛔ 不是订正一处已犯的错。⇒ ⛔ 不为凑订正而编造一个错误。 |
| ⛔⛔⛔ `LATCH-differentiation-falsified`<br>四条差异化逐条落空<br>(2026-08-23)<br>⭐ **必须进对外文档** | — | ⭐ **核实方法:⛔ 不看功能列表,看它们承认了什么**(因为「自曝边界」正是我以为的差异点)。证据 = 各项目 README 原文,`gh api repos/<r>/readme`,2026-08-23 取。<br>· ①「判据不能是常量」⇒ `ryangu00/axiom`:**A predicate is a letter, not a spirit.** `file_exists` passes on an empty file<br>· ②「静默失败必然累积」⇒ 同上:a memory system that **reported healthy for 13 days while silently not writing**<br>· ③「启用条件不得由被判对象提供」(C13)⇒ 同上:An agent with write access **can delete its own active claim** … raises the cost from free to deliberate; **it does not make it impossible**<br>· ④「自曝覆盖比例」⇒ 同上**更彻底**:阈值标定 **n=1** · FP/FN 率是「**v1.2 commitment, not a v1 claim**」· 独立的 `KNOWN-LIMITATIONS.md` 与 `FAQ.md`<br>⭐⭐ 最刺的一条:axiom 写「邻居 `nah` 在**公开**语料(101,194 次工具调用)上标定,**that is the better standard and we say so**」⇒ ⛔ **它主动承认竞品某一维度更强** —— nonconstant 至今没做过这件事。 | **存在。** | ⭐ **推导:**我把「自曝边界」当成差异化 ⇒ ⛔ 但那是**这个领域的成熟做法**,不是 nonconstant 的发明。⚠️ ⛔ 而我此前从未做过 prior-art 扫描 ⇒ ⭐ **差异化的断言一直没有证据支撑** —— 它是**未经查证的自我认知**,与 `LATCH-cost-estimated-without-root-cause` 同族。 | ⭐⭐ **一般化:凡「我们与众不同」的断言,须先做 prior-art 扫描;⛔ 未扫描的差异化 = 未经查证的断言。**<br>⚠️ **空间比预想拥挤**:axiom 的 prior-art 一节点名**六个**同域项目(`groundtruth` 1,272 turns · `nah` 101,194 调用公开语料 · `claimcheck` · `tdd-guard` · `oh-my-claudecode` · `planning-with-files` 5+ 运行时)。<br>⛔ **nonconstant 的分发形态最弱**:axiom 有 4 个运行时适配,nonconstant 只有一个 shell 安装器。<br>⭐ **可能仍成立的一条(⛔ 未穷尽,按 F3 标注覆盖范围 = 仅 README 正文)**:它们判「agent 的声明」,⭐ nonconstant 有一层判**判据系统自身**(`meta-gate` 未演示一过一失败者不得启用 · `waiver-expiry` 写下的到期须有判据 · `doc-budget` 写下的上限须有判据)。<br>⇒ ⛔ **定位必须重写,发布继续停。**详见 `docs/audit/21-PRIOR-ART.md`。 |
| ⛔⛔⛔ `LATCH-scan-top-result-only`<br>查重只看排第一的结果 ⇒ 漏掉同域项目<br>(2026-08-24)<br>⭐ **必须进对外文档** | — | ⭐ 2026-08-23 判 `recuse`「异域可共存」,依据是 GitHub 搜索的**第一个**结果 `xzycd/recuse`(Polymarket 预言机争议 CLI)⇒ ⛔ 判为异域。<br>⛔⛔ **全量扫描后发现 `mthamil107/Recuse`**(Apache-2.0,Python):「a published mini-standard (response framework) for **cooperative AI-access governance**. Servers emit an in-band deny signal; **compliant LLM agents recuse themselves**. The robots.txt analogue for live server access.」<br>⚠️ 另有 arXiv **2606.06460**「Will the Agent Recuse Itself?」· IETF draft · PyPI 已发 `recuse-signal`。<br>⇒ ⛔⛔ **同名 + 同领域(AI agent 治理)+ 同一个语义 punch line** —— ⭐ 选 `recuse` 的理由(自反语法 · 主体主动退出),正是它写进 IETF draft 的那个理由。 | **存在。** | ⭐ **推导:**查重的目的是回答「有没有同域项目占了这个名字」,⛔ 而搜索排名按 star / 相关度排,**与「是否同域」无关** ⇒ ⭐ 同域的小项目会排在异域的大项目后面 ⇒ ⛔ 只看 top = 用一个与问题无关的排序来截断证据集。 | ⭐⭐ **一般化:查重必须扫全部结果的**描述**,⛔ 不得只看 top;判别靠「它做什么」,⛔ 不靠「它有多少 star」。**<br>⚠️ 与 `LATCH-incomplete-set` 同族**第 24 次**。<br>⭐ 与 `invigil` 那次**同形但更坏**:invigil 是 GitHub 1★ 看似可用、PyPI 简介暴露同域 —— ⭐ 那次靠**补查**抓到;⛔ 本次是**下达方基于 top 结果批准了**,而执行方也据此写进了排序表。<br>⇒ ⛔ `recuse` **出局**。⭐ 改名定为 **`nonconstant`**,详见 `docs/audit/20-NAME-CANDIDATES.md`。<br>⭐ 现行规则见 `18-PROTOCOL.md` §3(**F3**:否定命题须覆盖所有已知形式)。 |
| ⛔⛔⛔ `LATCH-name-collision-blocks-release`<br>发布前查重:名字在**同领域**已被占<br>(2026-08-23)<br>⭐ **阻塞发布** | — | ⭐ **逐项实测,⛔ 非印象:**<br>· ⛔⛔ **同领域、同受众**:`nonconstantagent/nonconstant`「Control layer for autonomous AI agents —— safe actions run automatically, risky actions wait for approval」(开源,上过 HN);`runnonconstant.sh`「Nonconstant —— Mission Control for AI Coding Agents」(桌面控制面,**点名 Claude Code / Codex / OpenClaw**,策略执行 + 审计轨迹 + 签名执行记录,AGPL-3.0)<br>· ⛔ **GitHub 仓名 `nonconstant`** 被 `nonconstantbio/nonconstant` 占(**174 ★**,Python 生物信息学框架)<br>· ⛔ **PyPI `nonconstant`** 被 NonconstantBio 占,**v2.77.0 发布于 2026-08-18**(查重前 5 天,活跃维护)<br>· ⛔ **npm `nonconstant`** 已注册(`registry.npmjs.org/nonconstant` ⇒ HTTP 200,v0.0.1,2013 年遗留)<br>· ⚠️ `nonconstant` 本身是**通用英文词**(门闩;并发原语 `CountDownNonconstant` 系)⇒ ⛔ 普通搜索几乎不可能排到前面 | **存在。** | ⭐ 撞名分两种:①「同名不同域」⇒ 可共存;⛔②「**同名同域**」⇒ 用户搜到的是别人,而**定位词也一样**(gates / AI coding agents / policy)⇒ ⛔ 不是排名问题,是**身份问题**。⇒ 本例是 ②。 | ⭐⭐ **一般化:发布前的查重必须在「同领域」维度做,⛔ 只查「同名」不够。**<br>⚠️ ⛔ **改名成本已量(⛔ 不重复 A005 那次「未量成本就下结论」的错)**:「nonconstant」字样 **709 处 / 51 个文件**;结构性引用 = 目录 `.nonconstant/`(12 个文件)+ `nonconstant.yml` + 脚本内写死 **76 行** + 配置自引用 **15 处**;文档散文 **589 处**(⭐ 可机械替换)。⛔ **最贵的一块**:**11 份完成报告各绑一个 commit hash**,而改名会让它们记录的 `.nonconstant/` 路径与所绑 commit 不符 ⇒ ⭐ 只能**加订正注记**,⛔ 不能改历史。<br>⇒ ⛔ **发布已停,等改名决定。**⛔ 未写 A009、⛔ 未打标签、⛔ 未推送。 |
| ⭐⭐ `LATCH-f5-at-execution-layer`<br>F5 第一次应用在**执行层**<br>(2026-08-23) | — | pre-commit hook 若对 `criteria-guard` 判红也阻断 ⇒ ⛔ **装了 hook 的仓永远改不了自己的判据**(改判据必然让该闸判红,而 `git commit --no-verify` 又被环境的全局 hook 挡着)。⭐ 实测:首次提交 hook 自身即被自己拒绝。 | **存在。** | ⭐ **F5** 此前只在**行数预算**(A001)与**文件预算**(A003)上应用 —— ⭐ 本轮是第三次、也是**第一次在执行层**:任何自律型机制,须把「**修正自身**」排除在自己的执行之外。⛔ 否则与 `LATCH-self-referential-deadlock` / `LATCH-budget-eats-the-fix` 同形。 | ⭐ **做法**:`nonconstant.yml` 里给该闸加 `pre_commit: advisory` —— 判红时**大声打印但放行**。<br>⭐⭐ **关键在豁免放哪**:写在 `nonconstant.yml`(**受 `criteria-guard` 保护**)⇒ ⛔ 改这个豁免本身会判红;⛔ 若硬编码进 hook,它就成了不可见的逃逸口。<br>⚠️ ⛔ **自曝代价**:pre-commit 挡不住「偷改判据」—— ⛔ 但 hook 本来就挡不住(`git commit --no-verify` 是 git 自带的),⭐ **真正拦它的是任务期跑闸**(被判定者跑 `gates.sh` 时判红),⛔ 那条一点未被削弱。<br>⚠️ 两次真实事故(`status-facts` 红、`doc-budget` 红)**都不是 `criteria-guard`** ⇒ ⭐ 新 hook 对它们全数拦截。<br>⭐ 现行规则见 `18-PROTOCOL.md` §3(**F5**)。 |
| ⭐⭐⭐ `LATCH-hardening-recursion`<br>加固判据是对尚不存在的用户做优化<br>(2026-08-23)<br>⭐ **必须进对外文档** | — | Phase 10 收尾后**连续 7 轮**全部是元工作,⛔ 对 Phase 8(唯一能产生用户的事)**零进展**。⚠️ `01-PLAN.md` §7 写着「元工作时间占比 15%」触发精简 ⇒ ⭐ 实际 **≈100%**,⛔ 而那条阈值从未被任何东西检查。<br>⭐⭐ **信号被无视的实例**:`18-PROTOCOL.md` 撞上自己的 80 行上限 —— ⭐ 那本身就是「规则太多了」的信号,⛔ 而处置是**腾地方继续加**。 | **存在。** | ⭐ **推导:**加固判据 = 为一个**尚不存在的用户**优化 ⇒ ⛔ 代价是推迟**那件能创造用户的事**;⚠️ 而判据系统**永远可以更健壮** ⇒ ⛔ **无限递归,没有自然停点**。<br>⭐ **真实发生率**:自然发生 **1 次**(`status-facts` 断言 2 静默失效,且被**下一轮**扫出),⛔ 其余全部由探针刻意构造 ⇒ ⭐ **是脚枪(自己才会踩),不是漏洞(别人会踩)**。 | ⭐⭐ **一般化:凡「让工具更可靠」的工作,须先问它推迟了什么。**⛔ 无使用者时,可靠性的边际收益为零,⭐ 而机会成本是全部。<br>⭐ **处置(⛔ 不是「以后再说」)**:按 nonconstant 自己的主张 —— **承认边界并标注,⛔ 不假装完备**。四项缺陷各有 LEDGER 条目 ⇒ ⭐ **带着文档化的已知缺口发布**,见 `01-PLAN.md` §6 **Q19**。<br>⚠️ ⛔ **触发条件是「首个外部用户报告相关问题」,⛔ 不是「有空就做」** —— 后者会让递归立刻重启。 |
| ⭐⭐⭐ `LATCH-harness-failure-looks-like-red`<br>验证工具的失败伪装成红检通过<br>(2026-08-23)<br>⭐ **必须进对外文档** | — | 本轮红检用 `subprocess.run(['bash','-c',…])` 跑判据 ⇒ 解析到 WSL 的 bash,`execvpe(/bin/bash) failed` ⇒ **每一条都返回 1**。⭐ 我把「跑不起来」读成「判红 = 符合预期」,**打了四个 OK**。⛔ 是两个期望 0 的用例返回 1(XX)才逼我去看 stderr —— ⚠️ **若那批用例的期望值全是「非 0」,整跑会被完整记成通过。** | **存在。** | ⭐ **推导:**红检的结论是「判据在该红时红了」,而「红」是从**退出码**读的 ⇒ ⛔ 探针工具**自身失败**也返回非 0 ⇒ ⭐ **「判据判红」与「探针没跑起来」不可区分** ⇒ ⛔ 红检整类假通过 | ⭐⭐ **一般化:红检必须配基线自检;⛔ 否则「工具坏了」与「判据判红」不可区分,红检整类假通过。**<br>⚠️ 与 `LATCH-cannot-judge-vs-judged-fail` **同族**,⛔ 但作用在**验证层**而非判据层 ⇒ ⭐ **危害更大**:判据层那条让**一条**结论失去依据,本条让**所有**红检结论失去依据。<br>⭐ **方案(⛔ 本轮未实现)**:① 每批探针前先跑一条**必然绿**的基线,非 0 ⇒ 整跑作废;② 红检期望**具体退出码**,⛔ 不写「非 0」;③ ⭐⭐ **最强且最便宜**:红检同时校验判据打印的**原因串** —— 工具失败产生不了 `⛔ criteria-guard FAIL —— 被判定者改动了判据文件` 这种字符串,⭐ 它证明的是**判据真的跑了并且判了**。<br>⚠️ ⛔ 条款待立(**C14**),⚠️ `18-PROTOCOL.md` 已 80/80 ⇒ **须先腾地方**,见本轮报告。 |
| ⭐⭐⭐ `LATCH-self-blinding`<br>判据被自己的输入关掉<br>(2026-08-23)<br>⭐ **必须进对外文档** | — | 给 `nonconstant.yml` 的 `protected` 条目加**行尾注释**(`- nonconstant.yml            # 配置本体`)⇒ 提取出的 glob 含注释文本 ⇒ 恒不命中。⭐ 实测:`git status` 明明是 ` M nonconstant.yml`,而 `criteria-guard` 判 **0 PASS**。加引号(`- "nonconstant.yml"`)、只留 `- src/**` 同样判 0。 | **存在。** | ⭐ **推导:**`criteria-guard` 的**被判对象清单**存放在它自己**保护的文件**里 ⇒ 改清单 = 改 `nonconstant.yml` = 应被判红 ⇒ ⛔ 但改的内容恰好让它认不出 `nonconstant.yml` ⇒ ⭐⭐ **一次编辑同时完成「越界」与「让越界不可见」** | ⭐⭐ **一般化:判据的启用条件,不得由被判对象提供。**<br>⛔ ⚠️ **这不是 C12 的实例** —— C12 是「覆盖萎缩」(判据还在跑,只是少查了);⭐ 本条是**判据被自己的输入关掉**。<br>⚠️ 与 `LATCH-input-control`(base 由被判定者选)同族,⛔ **但更狠**:那里输入影响**结论**,⭐ 这里输入影响**判据是否运行**。<br>⚠️ ⛔ 且 `nonconstant.yml:7` 明文写着不得用 `src/**`「恒不命中 = 常量」⇒ ⭐ **那条规则本身就是无判据的**(= `LATCH-protocol-has-no-teeth`)。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4(**C13**)。⚠️ ⛔ **未修** —— 修法须 ①+③ 配套:「每条模式至少命中一个现存对象」+「显式声明允许为空」的通道。 |
| ⭐⭐ `LATCH-git-restore-wipes-work`<br>用 git 复原会连带冲掉未提交的工作<br>(2026-08-23) | — | 探针复原时用 `git checkout -- nonconstant.yml`,⛔ 把同一文件里**本轮尚未提交**的 `expected_assertion_classes` 一起还原掉了。⭐ 当场发现并重加(内容在手,⛔ 无损失)。 | **存在。** | ⭐ `git checkout --` 的语义是「回到 HEAD」,⛔ 不是「撤销我刚才那次探针改动」—— **它分不出哪些改动是探针的、哪些是本轮工作的**。 | ⭐⭐ **探针复原一律用仓外备份**(`cp file $TEMP/bk/` → 改 → `cp $TEMP/bk/file .`),⛔ 不用 `git checkout`。<br>⚠️ 适用面更广:凡**同一文件**上既有「要保留的改动」又有「要丢弃的改动」,⛔ 任何以 HEAD 为基准的复原都不安全。 |
| ⭐⭐⭐ `LATCH-pattern-miss-reports-pass`<br>匹配失败被报告成通过<br>(2026-08-23)<br>⭐ **必须进对外文档** | — | **同一轮内两次实例:**<br>① `status-facts` 断言 2 的正则是 `Phase 0~<n> 全部完成`;STATUS 措辞改成「Phase 0~7 **+ 10** 全部完成」后**匹配不上** ⇒ 该断言被跳过 ⇒ 判据仍报「10 条断言一致」**PASS**。<br>② 同一轮我新写的 amendments 断言用 `grep -o 'amendments/A[0-9]…'`,**把目录名写进正则** ⇒ 断言改成 `nosuchdir/A001~A007` 时匹配不上 ⇒ **判 0**(探针实测抓到)。 | **存在。** | ⭐ **推导:**判据用正则匹配文本来**定位被判对象** ⇒ ⛔ 「匹配失败」与「没有该断言」**不可区分** ⇒ 措辞一变则覆盖静默减少 ⇒ ⛔ 而它仍报 PASS ⇒ ⭐⭐ **判据的覆盖面本身是不可观测的** | ⭐⭐ **一般化:凡以「匹配」定位被判对象的判据,必须能区分「匹配失败」与「对象不存在」;⛔ 否则覆盖面会静默萎缩,而判据一路绿灯。**<br>⚠️ ⛔ **比 T5 更隐蔽**:T5 = 失败**不产生输出**;⭐ 本条 = 失败**被报告成成功** —— 观察者拿到的是**正向确认**,⛔ 不是沉默。<br>⛔ ⚠️ **两次都是我自己犯的,且在同一轮内** ⇒ ⭐ 这不是失误,是**该实现范式的固有缺陷**。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4(**C12**);⭐ 首个机器化落点:`status-facts` 的 `expected_assertion_classes`(M < N ⇒ 判红)。<br>⛔⛔ **2026-08-23 逐条实测:8 条判据里 6 条有同族缺陷(4 强 2 弱),⛔ 全部亲手复现、非推断:**<br>· ⛔⛔ `criteria-guard`:给 `protected` 条目加**行尾注释** ⇒ glob 变成 `nonconstant.yml            # 配置本体` ⇒ 恒不命中 ⇒ **改着 `nonconstant.yml` 却判 0 PASS**;加引号、只留 `src/**` 同样判 0。⭐⭐ **自遮蔽** —— 改清单那次编辑本身就是「改 nonconstant.yml」,而它恰好让判据认不出 nonconstant.yml ⇒ **一步之内禁用自己的检测**。⚠️ `nonconstant.yml:7` 明文写着不得用 `src/**`「恒不命中 = 常量」,⛔ 无任何机器判据。<br>· ⛔⛔ `doc-budget`:行尾注释被吞进 `exclude_sections`。**A/B**:`max_lines: 300` 下原样 ⇒ **1**(380/300 超限);把注释改成 `# …A001，另见 10` ⇒ **0 PASS** ⇒ ⛔ 改两个字排除掉 §10,**超限静默消失** = 藏账。<br>· ⛔⛔ `waiver-expiry`:`until:` 改成 `expires:` ⇒ 匹配 0 条 ⇒ 「一条豁免都没有」**PASS 0**。<br>· ⛔⛔ `report-pin`:报告不叫 `phase*.md` ⇒ `find` 命中 0 ⇒ 「**0 份**报告均绑到真实提交」**PASS 0**。<br>· ⚠️ `meta-gate`:`subjects:` 整块消失 ⇒ 循环不执行 ⇒ 不判红(⭐ `IDS` 空有守卫判 2)。<br>· ⚠️ `silent-scan`:目录里没 `.py`/`.sh` ⇒ 「扫描 **0** 个文件」PASS(⭐ 主体是「定位违规」,险只在文件类型这层)。<br>· ⭐ 无险:`upstream-semantics` · `upstream-pin`(行为探针 / `git rev-parse`,⛔ 不靠匹配定位)。<br>⚠️ ⛔ **实测暴露出第二种形态**:模式**匹配到了,但取出的东西无法履行职责** ⇒ 效果同样是覆盖静默归零。<br>⇒ ⛔⛔ **须统一处理,⛔ 不逐条打补丁。**⭐ 三条共同要求:① 清单类判据须证明**每条模式至少能命中一个现存对象**(⛔ 非空 ≠ 有效);② 配置值提取须**剥行尾注释与引号**(⛔ 当前一律原样吞下);③ vacuous PASS 要么守卫判 **2**,要么在 `nonconstant.yml` 里**显式声明允许为空**。 |
| ⭐⭐⭐ `LATCH-id-is-identity-not-order`<br>标识符一旦被引用就是身份<br>(2026-08-22,A007)<br>⭐ **必须进对外文档** | — | A007 要把 Phase 10 提到 Phase 8 之前。⚠️ 直觉做法是**重编号**(10→8, 8→9, 9+→10+)。⭐ **实测:重编号须同步 ≥ 7 处引用**(3 个 `### Phase` 标题 · `nonconstant.yml:75` 的 `until` 及其解除条件句 · `nonconstant.yml:123` `demo_pass` · `01-PLAN.md:461/488` 两处文内引用);⛔ 而**移动块、保留编号**须同步 **0 处** | **存在。** | ⭐ **推导:**编号若 = 顺序 ⇒ 顺序变则编号变 ⇒ ⛔ **所有引用失效**;⭐ 编号若 = **身份**(像 commit hash)⇒ 一经排定永不变 ⇒ 顺序改由**块的物理位置**承载 ⇒ ⛔ **零引用改动** | ⭐⭐ **一般化:标识符一旦被跨文件引用,它就是身份,⛔ 不再是序号** —— 任何让身份随排序变动的设计,都会让引用**静默失效**。<br>⚠️ 与 **C7**(ID 命名空间)**同族,⛔ 但更基本**:C7 管「⛔ 别撞号」,⭐ 这条管「⛔ 别改号」。<br>⭐⭐ **这是对 `LATCH-renumber-breaks-reference` 的根治,⛔ 不是绕过** —— 后者记的是「重排会打断引用」,本条给出「不必重排编号」。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4(C11) |
| ⭐⭐⭐ `LATCH-vendored-is-not-installed`<br>副本不等于用户实际运行物<br>(2026-08-22,A006)<br>⭐ **必须进对外文档** | — | nonconstant 此前**全部**关于 spec-kit 的结论都来自 **vendored 源码树**。⭐ 本轮首次对**真实安装**(`uv tool install specify-cli`,pin 同为 `bca6790`)跑 `upstream-semantics` ⇒ **0**,且**注入的依赖桩为「(无)」** —— ⛔ 而 vendored 树需要 **3 个桩**(`json5`/`readchar`/`pathspec`) | **存在。** | ⚠️ ⛔ **「读 vendor」与「跑真实安装」是两个不同的对象。**⇒ Phase 6 的语义结论**虽对**,⛔ 但**被测对象不是用户会遇到的那个**。⭐⭐ **现在验过、结果一致 —— 那是运气好,⛔ 不是设计保证。** | ⭐⭐ **一般化:对「副本」的验证不能替代对「用户实际运行物」的验证** —— ⚠️ 二者可能行为不同,⛔ **且差异是静默的**(结论看起来一样)。<br>⭐ 已立协议 **C10**。⚠️ Phase 6 报告 `known_gaps` #2 的措辞据此**过宽**,订正列入 Phase 8 第一批(A006 `known_gaps` #2)。<br>⚠️ 与 `LATCH-fixture-cannot-carry-identity` **同族**:那条说「fixture 复制不了身份」,⭐ 这条说「**副本复制不了运行环境**」。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4(C10) |
| ⭐⭐⭐ `LATCH-count-mismatch-must-be-chased`<br>对不上的数字必须查到根因<br>(2026-08-22,Phase 7) | — | 转 submodule 后 `vendor/spec-kit` 文件数 **639 → 543**。⛔ 未当成正常差异,逐个 diff 到底:差异**全是** Phase 6 探针生成的 `__pycache__/*.pyc`(97 个)+ `.git`;共有 **542 个文件 `diff -r -q` 零内容差异** | **存在。** | ⚠️ ⛔ **若当时放过,此前所有对 vendor 的实测都失去依据** —— Phase 6 的语义验证、全部对 spec-kit 的结论,⛔ 悉数悬空 | ⭐⭐ **一般化:一个对不上的数字,必须查到根因;⛔ 把它归为「正常差异」而不查,会让所有基于该对象的结论一起失效。**<br>⚠️ 与 **F1** 同族,⛔ **但更强**:F1 说「以实测为准」,⭐ 这条说「**实测出现矛盾时,不许绕过**」。<br>⚠️ ⛔ **同轮反例:**协议分类的验证 grep 给了**假阴性**(5 行未分类却报「剩余未分类: 0」),⇒ ⭐ **本条对「验证脚本自身」同样适用**。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §3 |
| ⭐⭐ `LATCH-fixture-cannot-carry-identity`<br>fixture 复制得了内容,复制不了身份<br>(2026-08-22,Phase 7) | — | Phase 7 红检**必须动真 submodule**(切 commit / 改文件 / 塞新文件),⛔ 违反 **C3**「红检对 fixture 跑」的字面 —— ⭐ 因为**副本不是 submodule**,无法复现被测性质 | **存在。**⚠️ ⛔ **不是第一次** —— DevLoop 侧「fixture 必须用**真闸文件**」是同一形状 | ⛔ 若坚持用副本,红检验的是「副本的身份」⇒ **对真源零信息量** | ⭐⭐ **一般化:fixture 能复制内容,⛔ 不能复制身份**(是不是 submodule / 是不是真闸)。⇒ ⭐ **当被测性质就是「身份」时,必须动真源;⛔ 此时 C3 让位**,⚠️ 但须:① **当场复原** ② **复原后复跑绿检** ③ **报告中声明**。⇒ 已立协议 **C9**。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4(C9) |
| ⭐⭐⭐ `LATCH-protocol-has-no-teeth`<br>写进协议不等于会被执行<br>(2026-08-22)<br>⭐ **必须进对外文档** | — | **F6**(「未量根因不得作为放弃依据」)立于 `593a188`,⛔ **下一轮下达方即违反它** —— 以未经查证的「仓库暴涨」否决 submodule(`LATCH-unverified-ban-forced-deadlock`) | **存在。** | ⭐ **与 `LATCH-uncheckable-limit` 同族:没有判据的规则靠人记,而人记不住。**<br>⚠️ ⛔ **但射程须分两类,⛔ 不可笼统:**<br>· ⭐ **可机器化的** —— 理论上写得成判据,⛔ 目前没写 ⇒ **这是缺口,不是边界**(例:**F2** 引用 ID 以磁盘为准 · **K2** §8 只增不删 · **K3** 不 import DevLoop · **C4** 判据不得引用不存在的路径 · **C7** ID 命名空间)<br>· ⛔ **不可机器化的** —— 「有没有量根因」判不了 ⇒ **D5,是边界不是缺口**(例:**F6** · **R4** 发现必须说 · **K4** 白名单式 `git add` 事后无痕 · **K5** 先落盘再讨论) | ⭐ **已逐条标注**(`18-PROTOCOL.md` 各表新增「判据」列,⛔ 0 新行)。⚠️ ⛔ **订正(⛔ 原数不删):首次统计为「23 条 · 缺口 10 · D5 9 · 已机器化 3」—— 错。**`K1 K3 K5 C3 C4` **五行根本没被分类**,而验证用的 grep 给了**假阴性**(规则正文本身以 ⛔/⭐ 开头,被误判成已分类)。⇒ ⭐ **正确分布(二次订正后,含新增 C9):共 29 条 —— ⚠️ 缺口 11 · ⛔ D5 12 · ⭐ 已机器化 5 · 许可 1。**⚠️ 二次订正含两处:① **R8** 由「缺口(无载体)」改判 **⭐ 已机器化(判定者=用户)** —— 它的对象是**下达方的回复**,⛔ 确实不在仓库里,⭐ 但**用户看那一节在不在,就是判据**,⇒ 不是无判据,是**判据在用户那一侧**;② **C9** 我一度标成「已机器化(惯例)」—— ⛔ **错**,「被测性质是不是身份」是**判断** ⇒ 改 **D5**。<br>⇒ ⭐⭐ **这直接回答了「还能补几条判据」:11 条。**⛔ 本轮只标注与排序,不实现。<br>⭐⭐ **该比例须进对外文档。⛔ 隐去它会让 nonconstant 被误读为完备约束系统** —— 29 条里只有 **5** 条是真正硬的。<br>⭐ **优先级(可机器化 × 已咬过人):****① F2** 引用 ID 以磁盘为准 —— ⭐ 咬过 **4 次**,判据只需扫全仓 `LATCH-*`/`A00N` 引用是否存在 ⇒ **最高**;**② C7** ID 命名空间 —— 咬过 1 次(C6 撞号);**③ K6** 契约变更走 amendment —— 咬过 1 次(§8 #13 无声契约变更),可判「改 `CLAUDE.md` 的 commit 是否同时含 `amendments/` 新文件」;**④ K3** 不 import DevLoop —— ⛔ 未咬过,⚠️ 但一旦发生代价极高,且 grep 即可;**⑤ K2** §8 只增不删 · **⑥ C4** 判据不得引用不存在的路径 · **⑦ C1**(部分已由 `meta-gate` 覆盖);**⑧ C6** 127 —— ⭐ 咬过 **5 次**,⛔ 但缺口在**编排层** ⇒ 依赖 Phase 9+;**⑨ R2/R5/R7** 与已有判据重合。⚠️ ⛔ **原列第 ⑩ 位的 R8 已移出缺口**(改判「已机器化,判定者=用户」)⇒ 排序只剩 ⑨ 位、共 **11** 条<br>⚠️ 已机器化的 3 条是 **C2**(`meta-gate`)· **C5**(A004)· **C8**(`doc-budget`);**K1**(不改上游)由 **Phase 7** 从缺口转为已机器化。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4 |
| ⭐⭐⭐ `LATCH-unverified-ban-forced-deadlock`<br>未查证的禁令逼出死结<br>(2026-08-22,Phase 7 排期) | — | 下达方写「⛔ 不把 vendor 纳入版本控制」,理由两条,⛔ **两条都没查**:「仓库暴涨」·「与只读参考矛盾」。⇒ 该禁令**排除了唯一非代用品的解法**(submodule),把 Phase 7 逼成死结;执行方先查三条路后报告,才暴露出来 | **存在。** | ⭐ **实测两条理由全错:**<br>· 「仓库暴涨」⇒ ⛔ **提交进版本库的净负载 = `.gitmodules` 107 字节 + 一个 gitlink 条目**(实测于 scratch 仓)。⚠️ 量级差约五个数量级<br>· 「与只读参考矛盾」⇒ ⛔ **不矛盾** —— submodule 恰恰是「**指向别人仓库的只读引用**」,它就是为这个设计的 | ⚠️ ⛔ **这是 `LATCH-cost-estimated-without-root-cause` 的直接复发** —— **未量成本就否决方案**。⇒ 同族**第 19 次**。<br>⭐⭐ **F6 立于该条之后一轮,⛔ 下达方随即违反了它** —— ⚠️ 说明**写进协议不等于会被执行**(与 `LATCH-uncheckable-limit` 同族:没有判据的规则靠人记)。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §3(F6) |
| ⭐⭐ `LATCH-renumber-breaks-reference`<br>改编号会让引用指向语义已变的 phase<br>(2026-08-22,Phase 7 排期) | — | §10 插入 Phase 7 后,原 `Phase 9 · 消除自扫豁免` 与顺延后的 `Phase 9+ · 编排层` **撞号** ⇒ `nonconstant.yml` 里 `until: Phase 9 —— 自扫豁免被【消除】` 会指向一个**语义已变**的 phase | **存在。**由执行方在改编号**之前**查出 | ⛔⛔ **而 `waiver-expiry` 照样判绿** —— 它查「是不是具体 phase / 过没过期 / 排没排期」,⛔ **查不了「指的是不是同一件事」** | ⭐⭐ **这是 `LATCH-criteria-cannot-test-reasoning` 的第一个实例**:判据能测「声明与事实一致」,⛔ 测不了「声明指的还是不是原来那件事」。<br>⇒ **处置:**Phase 9 → **Phase 10**,`until` 同步改;⛔ **只换数字不够,须保留那句解除条件**。<br>⚠️ ⛔ **未解:目前靠人在改编号时记得同步。**<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4 |
| ⭐⭐ `LATCH-cannot-judge-vs-judged-fail`<br>「判不了」≠「判定为失败」<br>(2026-08-22,Phase 6) | — | Phase 6 探针 B/C:上游目录**结构完全不同** / `gate` step **整个被删** ⇒ 判据**导不进**上游模块。⭐ 判 **2**(闸自身故障/判不了),⛔ **不判 1** | **存在。** | ⛔ 判 `1` 等于断言「**语义已变**」—— 而结构对不上时**根本没测到语义** ⇒ **过度声称**。⚠️ ⛔ 反过来,把「判不了」混进「通过」更坏(那是 C6 防的) | ⭐⭐ **一般化:退出码必须区分「判定为失败」与「无法判定」。**⛔ 混为一谈会让「**我没测到**」被读成「**我测过了,不合格**」—— 前者需要人去看,后者不需要。<br>⚠️ 不变量:**两者都非 0** ⇒ ⭐ 什么都不会被静默放行。<br>⚠️ 与 `dossier.py:147`「SKIP/VOID ＝ 没验,⛔ 不是验过没事」**同族**(方向相反的一面)。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4(C6) |
| ⭐⭐⭐ `LATCH-criteria-cannot-test-reasoning`<br>判据测不到推理<br>(2026-08-22)<br>⭐ **必须进对外文档** | — | Phase 5 收尾的观察:**判据只告诉你「这条到期不合格」,⛔ 它不会告诉你「原因估错了」。**解开死结的是一次**证伪**,⛔ 不是判据 —— 那一步仍靠人 | **存在。** | ⭐ **推导:**判据能测「**声明与事实是否一致**」;⛔ 测不了「**声明背后的推理是否成立**」。⇒ 「要真解析器才能消」在**形式上完全合规** —— 它就是一个 `until`,判据对它没有意见。⇒ ⭐ **错的是推理,而推理不在判据的射程里。**<br>⚠️ **实证(⛔ 按可核数,非按记忆):**该错误估算跨 **26 个 commit**(`5a3420d`→`30005dc`,同日)被**全绿放行**,直到有人去量根因 | ⭐⭐ **一般化:判据保证的是「声明可核」,⛔ 不是「判断正确」。**<br>⚠️ 与 **D5** 同族,⛔ **但更具体**:D5 说「有些东西写不成判据」;⭐ 这条说「**写成了判据的东西,其前提仍不在判据射程内**」。<br>⭐⭐ **必须进对外文档** —— 它比「nonconstant 有几条判据」值钱得多,⛔ 且它防的正是读者把 nonconstant 当成「装上就不会错」的东西。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4 |
| ⭐⭐⭐ `LATCH-cost-estimated-without-root-cause`<br>没量根因就估成本<br>(2026-08-22,`5105ae2`) | — | Phase 2 收尾判定「自扫豁免**要真解析器才能消**」,并把该结论写进 waiver 的 `until`(「引入真解析器后移除(⛔ 解析器会让 103 行变上千行…)」)。<br>⭐ **2026-08-22 实测证伪**(`$TEMP` 内,⛔ 未动真源):自扫命中**恰好 2 处**,`scan-silent.sh:64` `:65`,**都在 `printf` 的消息文本里**,⛔ **不在正则里**。⇒ **只改那两句措辞:自扫 ⇒ `0`,红检对脏 fixture 仍 ⇒ `1`(2 处命中)。** | **存在。** | ⛔ 一个基于「**我以为的原因**」的成本估算,**高估了两个数量级**(「上千行」⇄ 实际 **0 净增**)。<br>⚠️ ⛔ 更坏的是:该结论**当时被接受了,并写进 waiver 的 `until`** ⇒ **一个错误估算变成了一条长期债务** | ⭐⭐ **一般化:凡「这个做不到 / 太贵」的断言,须先量根因;⛔ 未量根因的成本估算不得作为放弃的依据。**⚠️ 与 DevLoop 侧「凡『根因是 X』先问它能否解释量到的分布」**同族** —— 两者都是**先验证前提,再采信结论**。<br>⚠️ ⛔ **转述本条时又犯了一次:**下达方给出未经核实的「**18 天**」,由执行方按 **F1** 核出 `5a3420d` 与 `30005dc` **同日**,实测为**跨 26 个 commit**。⇒ ⭐ **`LATCH-cite-from-memory` 同族第 18 次**,⚠️ ⛔ **且错的正是「本条要记进 LEDGER 的内容」本身**。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §3(F6) |
| ⭐⭐ `LATCH-untracked-invisible`<br>未提交的东西对 criteria-guard 不存在<br>(2026-08-22,Phase 5 探针 E) | — | Phase 5 探针 E:删掉 `.nonconstant/waiver-expiry.sh` 后 `criteria-guard` 判 **0**(⛔ 没看见)。⚠️ 复验:该文件当时**尚未提交**;提交后再删 ⇒ 判 **1**(正常) | **存在。** | ⭐ **根因比「窗口很窄」更深:**`criteria-guard` 比对的是 `git diff HEAD` ⇒ ⛔ **它只能看见「已进入版本控制的东西」的变化** ⇒ 一个**从未提交的判据,对它而言不存在** ⇒ 删掉它不产生任何可观测输出(**T5 形状**) | ⚠️ ⛔ **本轮不修**(Phase 5 未闭,且改 `gates.sh` 需授权)。⇒ Phase 5 关闭后处理。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4 |
| ⭐⭐⭐ `LATCH-criteria-cross-check`<br>判据开始互相约束<br>(2026-08-22,`8f18e52` → `900b8de`) | — | Phase 4 新建 `.nonconstant/status-facts.sh` 后,**`silent-scan`(Phase 2 的判据)在它里面抓到两处真的静默失败**:`:45` 的 `2>/dev/null`、`:62` 的 `\|\| true`。⇒ ⭐ **修代码,⛔ 未开豁免**;⛔ 且因首个 lock `8f18e52` 已含该缺陷,**报告改绑 `900b8de`** —— 绑一个已知有缺陷的 commit 会让「Phase 4 通过」这句话失真 | **存在。** | ⭐⭐ **这是 T1 的一次质变。**推导:判据之间互相检查 → 新判据的作者是**被判定者** → ⭐ 而检查它的是**另一条已存在的判据** ⇒ **判定者独立于被判定者(T1),且是自动的** | ⚠️ **对照:**此前 16 次错误全靠 nonconstant session 拦下达方 —— ⛔ 那是**角色**在判。⇒ ⭐⭐ **这是第一次由判据本身拦下判据:T1 从「靠角色分离」变成「靠机制」。**<br>⭐ **一般化:判据集合到达一定规模后,会开始互相约束 —— 这比任何单条判据都强,⛔ 且不依赖任何人记得去检查。**<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4 |
| ⭐⭐⭐ `LATCH-uncheckable-limit`<br>从不被机器检查的上限<br>(2026-08-22,`45cba9b`) | — | `STATUS.md` 抬头写着「⛔ 上限 60 行」,⛔ **从未被任何东西检查** —— 进入本轮时已 **77 行**,⛔ 无人知晓。<br>⭐ **根因:**`01-PLAN.md` 400 与 `18-PROTOCOL.md` 80 之所以「有效」,⛔ **不是因为有判据**,而是因为**下达方每轮要求报行数** ⇒ ⭐⭐ **人肉判据,换 session 即失效**。`STATUS.md` 从来没人要求报 ⇒ 它就漂了。<br>⚠️ ⛔ 同时暴露:**三个文档的上限当时全部依赖人肉执行。** | **存在。**由执行方在 Phase 3 收尾自行报出 | ⭐⭐ **一般化:一个从不被机器检查的约束,等于不存在 —— ⚠️ 而且比「没有约束」更坏,因为它让人以为有约束。**⇒ 与 §3 序 4(判据必须仍是判据)、`A001` `known_gaps` #1 同族 | ⇒ **处置(`45cba9b`):**建 `.nonconstant/doc-budget.sh`,⛔ **不是只给 STATUS 建一条** —— 一次覆盖三个文件,从 `nonconstant.yml:doc_budgets` 读「文件 → 上限 → 计数范围」,支持 A001 的 `exclude_sections` scope 模型;级别 **soft**,超限须登记 `waiver_reason` + `waiver_until`(⛔ 须具体 phase)且**运行时打印**。<br>⭐ **建成当场就抓到了它:**首跑即报 `STATUS.md 77/60,超 17 行`。<br>⚠️ ⛔ **更严重的发现:STATUS 不只超限,内容是错的** —— 它当时写着「⛔ 零行代码」(实为 5 条判据已提交)、「Phase 0 可以开工」(实为 0~3 全部完成)。⇒ ⭐ **故选「精简 + 订正」而非「登记 waiver」:waiver 会把错误内容一并保住。**<br>⚠️ ⛔ **行数判据抓得到长度,抓不到失真。**⭐ **但「失真」须分两种,⛔ 不可笼统:**<br>· `01-PLAN.md` 的失真 ⇒ ⛔ **不可判定(D5)** —— 它写的是「**该怎么做**」,⛔ 没有对应物可比对<br>· ⭐ `STATUS.md` 的失真 ⇒ **可判定** —— 它写的是「**现在是什么状态**」,而状态由**仓库**决定,几乎每条断言都有对应物:「零行代码」⇔ `ls .nonconstant/*.sh` ·「最后 commit」⇔ `git rev-parse --short HEAD` ·「Phase 0 未开工」⇔ `ls reports/phase0-*.md` ·「B6 用了 N」⇔ `nonconstant.yml` 名额表<br>⇒ ⭐⭐ **STATUS 的失真不是 D5,是「还没做」** ⇒ 已排为 **§10 Phase 4**。<br>⚠️ ⛔ **订正(2026-08-22,Phase 5 收尾;⛔ 原文不删):上句过宽,须收窄。**准确表述:⭐ **STATUS 中「可对照仓库对象」的断言可判定**(判据条数 / 已完成 phase / 文件路径 —— 这三类正是 Phase 4 所实现);⛔ **「某机制是否已实现」这类断言不可判定** —— 它**没有唯一的仓库对应物**。<br>**实证:**STATUS 曾写「waiver 机制仍未实现」,在 `waiver-expiry` 落地后即为假,⛔ 而 `status-facts` 抓不到 —— 发现它的仍是人读了一遍。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4(C8) |
| ⭐⭐⭐ `LATCH-hardcoded-escape-hatch`<br>把逃逸口硬编码进源码<br>(2026-08-22,`cad8012` → `4785f73`) | — | Phase 3 的元判据要求每条判据登记「演示报告」,⛔ **但源码里对 `*PENDING*` 有一条白名单**(`.nonconstant/meta-gate.sh` 的 `case` 分支)。⇒ 一条判据可以**永远声称 PENDING** 而照样启用 ⇒ ⛔ **这正是元判据要防的那件事**。<br>⚠️ ⛔ 且它**硬编码在源码里**、不在 `nonconstant.yml` ⇒ **不受 criteria-guard 保护** —— 改它不判红 | **存在。**由执行方在 Phase 3 收尾**自行报出**(`reports/phase3-cad8012.md` `known_gaps` #2),用户裁定必须修 | ⛔ 判据可无限期「待验证」而照样启用 ⇒ 元判据被自己开的口子架空 | ⭐⭐ **同一问题的正确解法就在隔壁:**Phase 2 的自扫豁免 —— 写进 `nonconstant.yml` · 有 `reason` · 有 `until` · 受 criteria-guard 保护 · 运行时打印「已豁免」。⛔ **PENDING 白名单一样都没有。**<br>⇒ **处置(`4785f73`):**① 两处占位符先换成真实报告 `reports/phase3-cad8012.md`(`b4269a7`,⭐ 改判据当场判红 —— **那是对的**,走 phase 边界流程提交);② 白名单**从源码移除**。⛔ **未新建豁免机制** —— 现无任何判据需要「暂未演示」这个状态,建了就是投机性机器(YAGNI)。⭐ 若将来确需,必须走 `nonconstant.yml` 豁免形态:显式列判据 · reason · **until 须是具体 phase,⛔ 不得是「以后」** · 运行时打印。<br>⚠️ **辨析:**`.nonconstant/report.sh` 里也有 `*PENDING*` 分支,⛔ **但那是判红(deny),不是放行(allow)** —— 实测对 `phase9-PENDING.md` 判 `1`。⇒ ⭐ **allow 型白名单才是逃逸口;deny 型只是更清楚的报错。**<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4 |
| ⭐⭐ `LATCH-waiver-must-announce`<br>静默的豁免等于没有豁免<br>(2026-08-22,`34cc98f`) | — | Phase 2 收尾给 `silent-scan` 加了显式豁免(`.nonconstant/scan-silent.sh` 自扫必红,因文本匹配器必然匹配到自己源码里的模式串)。⭐ **实现时选择在运行时打印「⚠️ 已豁免(nonconstant.yml 显式登记): <路径>」**,⛔ 而非静默跳过 | **存在。** | ⛔ **一个静默的豁免,等于没有豁免** —— 它会变成「**这里本来就没检查**」。⇒ ⭐ 读者无法区分「此处已豁免」与「此处从未被检查」⇒ **T5 的形状**(状态存在但不产生可观测输出) | ⭐⭐ **豁免必须在运行时可见,⛔ 不得静默跳过。**⚠️ 与 `LATCH-missing-gate-is-silent` 同族:一个是**闸没了**没人知道,一个是**闸被跳过**没人知道。<br>⭐ 另两条一并做对:豁免的 `reason` / `until` 是 **YAML 字段**,⛔ 不是注释 ⇒ 落在受 Phase 1 测试守卫保护的 `nonconstant.yml` 内 ⇒ **改豁免 = 改判据 ⇒ 判红**。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4 |
| ⭐⭐ `LATCH-missing-gate-is-silent`<br>删掉闸门不产生任何输出<br>(2026-08-22,**两次复发**) | — | **闸门/扫描器被删 → shell 返回 `127`(command not found),⛔ 不是 nonconstant 的 `2`。**<br>**两次实测复发:**<br>· Phase 1 探针 —— 删 `.nonconstant/gates.sh` 后跑 ⇒ **127**(`reports/phase1-ae6f82f.md` `gate_results` #7)<br>· Phase 2 探针 E —— 删 `.nonconstant/scan-silent.sh` 后跑 ⇒ **127**(`reports/phase2-5a3420d.md` `gate_results` #7) | **存在。** | ⛔ 编排层若把「命令不存在」理解成「**没配这个检查 ⇒ 跳过**」⇒ **删掉闸门 = 流程绿着往下走**。⚠️ ⭐ **这是 T5 的形状:删掉一个检查,不产生任何可观测输出** ⇒ 无人知晓 ⇒ 累积 | ⭐⭐ **「闸门不存在」必须比「闸门判红」更严重。**⇒ 已立协议条款 **C6**;⚠️ 与 `dossier.py:147`「SKIP/VOID = 没验,⛔ 不是验过没事」同族。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4(C6) |
| ⭐⭐ `LATCH-cite-from-memory`<br>引用"我说过的名字",而非"磁盘上有的条目"<br>(2026-08-22) | — | 同一轮指令里三处同源,⚠️ **且发生在自查「不完整集合」的那一轮**:<br>**①** 三个数互不相同 —— 开头「共 **4** 条」· §四「**6** 条」· 实际列出 **#2~#7 = 6 条**,加 #1 sub-agent 禁令 ⇒ **7 条**。⇒ ⚠️ `LATCH-incomplete-set` 同族**第 11 次**<br>**②** 引用 `LATCH-ban-by-identity-not-action` —— ⛔ **该条目不存在**(磁盘实测 18 个 ID,无此项)⇒ ⛔ **引用不存在的出处第 3 次**(前两次:`07-REPLY-to-06.md`、`13-CONVERGED.md`,见 `01-PLAN.md` §8 #13)<br>**③** 编号从 #2 起,**#1 无定义** ⇒ 同族<br>⭐⭐ **第 4 次当轮复发**:本轮指令又称「`LATCH-blanket-ban-instead-of-gating` 说禁令保留,与 `LATCH-ban-by-feature-not-cause` 矛盾」—— ⛔ 磁盘实测 `blanket-ban` **0 命中**,该条目不存在;且 `LATCH-unexecutable-constraint` 记的是「临时目录铁律不可执行」,**与禁令存废无关** ⇒ ⛔ **所述矛盾在磁盘上不存在** | **存在,4 次。** | ⭐ **根因:下达方把「上一轮 prompt 里写过的名字」当成「已落盘的条目」** ⇒ ⛔ 记的是**我说过什么**,不是**仓库里有什么** | ⭐⭐ **规矩扩展:**`LATCH-count-from-memory` 原为「凡『有多少个 X』的断言以磁盘为准」⇒ **现扩为「凡引用任何条目 ID,同样以磁盘为准」**。<br>⭐ 执行方三次处置均正确:① 按文本实测判为 7 条并注明「以文本为准」,⛔ 未替下达方选数字掩盖分歧;② 绑到真实存在的 `LATCH-unexecutable-constraint` 上;③ 判定 #1 = sub-agent 禁令并写明「该判定非原文给出」。⛔ 第 4 次未构造指针到不存在的条目,而是报告。<br>⭐⭐ **第 4 次的根因比前三次更远一层:**前三次 = 把「**我说过的**」当成「已落盘的」;⛔ 第 4 次 = 把「**我打算让你记的**」当成「你已经记了的」。⚠️ 且发生在**记录该模式的同一轮**;⭐ 由执行方按 **F2** 先 grep 全部真实 ID 再动笔而拦下。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §3(F2) |
| ⭐⭐⭐ `LATCH-ban-by-feature-not-cause`<br>禁特征而非禁原因<br>(2026-08-22,⛔ **由用户质询触发,非自发**) | — | 下达方自查全部约束,撤回**同类禁令**。⚠️ **实测清单(⛔ 以文本为准,非以记忆为准)共 7 条:**<br>**#1** 「⛔ 不派 sub-agent」(此前已撤,见 `LATCH-unexecutable-constraint`)<br>**#2** 「⛔ 不许用模糊表述凑数」—— 判据是「措辞像不像模糊」⇒ ⛔ 机器判不了(D5),且诱导「把模糊的写得像具体的」<br>**#3** 「⛔ 不许压缩措辞蒙混」—— ⚠️ **已被绕过**:两条未决各压成一行塞入(Goodhart)<br>**#4** 「⛔ 不要写摘要」—— 顺带禁掉了有用的交接<br>**#5** 「⛔ 不要提出新方案」<br>**#6** 「⛔ 不要自作主张」(与 #5 叠加 ⇒ 实际效果 = **发现了也别说**)<br>**#7** 「⛔ 不要新增任何未在上面列出的条目」 | **存在。** | ⭐ **共同根因:看到一次坏结果 → 找到最近的相关特征 → 禁掉那个特征 ⇒ ⛔ 特征 ≠ 原因。**<br>⚠️ 与 DevLoop 侧一般化**同源**:「凡『根因是 X』的断言,先问:它能解释量到的分布吗?」<br>· 「sub-agent 会越权」⛔ 解释不了「主 session 也会」<br>· 「模糊措辞导致凑数」⛔ 解释不了「具体措辞也能凑数」<br>⚠️ ⛔ **最严重的是 #7:把下达方的盲区变成执行方的禁区** —— 而下达方**已 10 次给出不完整集合**。<br>⭐ **#5 在禁止最有价值的输出** —— 执行方提案中至少三条优于下达方原案:T5 同形那句(使自指死锁变为可推导)· `LATCH-budget-eats-the-fix` 的一般化 · §7a 分区口径(按「是否每次都读」而非字面枚举) | ⭐⭐ **母题:约束必须绑在可观察的动作上;绑身份、绑措辞形式、绑「态度」,都会被换个形式绕过,且会连带禁掉正当行为。**<br>⭐ **保留的约束及其共同点** —— 判据不通过就停下 · ⛔ 不改 `vendor/spec-kit` · §8 只增不删 · ⛔ 不 import DevLoop 一行 · 白名单式 `git add` · ⭐ 发现指令不可执行就停下报告(**这条救了下达方至少 4 次**)⇒ **全部绑在可观察的动作上。**<br>⭐⭐ **替代性通用铁律(本条即其落盘处):**① ⭐ 可以派 sub-agent,只读任务鼓励派;② ⛔ sub-agent 与主 session 同样:不得改判据、不得改 `01-PLAN.md`、不得 commit;③ ⚠️ sub-agent 产出文件,主控**必须抽验引用**后才采信;④ ⭐ **发现必须说;实施须授权**;⑤ ⭐ **发现指令的清单不完整或不可执行,必须报告**;⑥ ⭐ 提案可以提,须标为提案,⛔ 不得自行实施;⑦ ⛔ 判据不通过就停下,不许改判据让它通过。<br>⛔ 本条目是**档案**(只增不删),铁律会被订正,冲突时以 `18-PROTOCOL.md` 为准。<br>⭐ 现行操作规则见 `18-PROTOCOL.md` §1 · §5 |
| ⭐⭐ `LATCH-ledger-extraction-gap`<br>够格升格却没升格 = 无输出的待处理状态<br>(2026-08-22) | — | ⭐ **这是 T5 的一个新实例,⛔ 不是新问题。**推导链:LEDGER 只增不减、无人复查 → 够格升格却没升格 = **一个未被处理的状态** → 该状态**不产生任何可观测输出** → 无人知晓 → 无人处理 → 累积 ⇒ **必然退化成没人读的东西** | **存在。**⚠️ `T6 候选` 本身就是活证据:它躺在 §6 里指向 LEDGER,⛔ 没有任何机制会去处理它 | ⭐⭐ **一般化:T5 不只覆盖「失败被静默」,还覆盖「任何不产生输出的待处理状态」。**⚠️ 与 `LATCH-fix-cost-monotonicity` **同母题两侧**:修复成本上升 ⇒ 越来越不敢修;⭐ 提取从不发生 ⇒ 越攒越读不完。⇒ 都**不依赖发生率,只依赖速率 > 0** | ⛔ **不立 Q18** —— 记未决的收益 = 防它漂,而 `T6 候选` 已在 §6 且指向 LEDGER ⇒ Q18 只是把「这里有个缺口」再说一遍,**增量信息 ≈ 0**。⇒ ⭐ **最小动作:`T6 候选` 那一行就地扩写归属与触发(Phase 3 后,与 Q17 同批)。**<br>⭐ **将来的最小可行版本(⛔ 现在只记,不实施):**phase 完成报告加一必填字段「**本 phase 是否产生新的定理候选?**」(有则列出 + 指向 LEDGER;无则写「无」)⇒ ⛔ 不需要新判据、不需要新文件。⚠️ 但它**改 B5**(Phase 0 冻结契约)⇒ ⛔ **走 amendment**;⚠️ 该 amendment 文档写进 `amendments/`,**不新增 B6 名额** —— ⛔ 但理由是「**目录整体计数**」,⛔ 不是「不涉及文件」<br>⭐ **发现者 = 执行方**,⛔ 不在授权范围内,⭐ **但未越权行动,而是报告等裁定** ⇒ ⭐⭐ **这正是 D3(分级放行)想要的形状:发现越权,但不越权行动** —— 执行方实践了 PLAN 里尚未实现的机制。<br>⚠️ ⛔ 下达方原写「**首次**由执行方自发提出」—— **未经核查,撤回**。此前至少三次:Phase 0 `known_gaps` #3 · Phase 1 base 控制 gap · §7a 分区口径。⇒ ⚠️ **`LATCH-incomplete-set` 同族第 10 次** <br>⭐ 现行操作规则见 `18-PROTOCOL.md` §1(R4) |
| `LATCH-directive-self-audit-3`<br>下达方本轮自审:三处错<br>(2026-08-22) | — | ① 引用 `LATCH-budget-eats-the-fix` 论证「**绕开预算**」—— ⛔ 该条的正确结论是**改预算规则**(把「记录自身缺陷」排除在计数外),⛔ 不是绕开预算。**用一条规矩论证了一个规避动作。**<br>② 「余量 105 不是理由」**方向反了** —— 该句立意是「规矩不因有空间而不执行」,⛔ 却被用来支持「少记录」,与立意相反。<br>③ 「首次由执行方自发提出」未经核查(见 `LATCH-ledger-extraction-gap`) | **存在,三处。**⚠️ 判定者:下达方**自审** | — | ⚠️ ⭐ **下达方自审有效,⛔ 但仅在被外部要求时触发** —— 本轮三处错是在用户指出「没跑验证」之后才找到的。⇒ ⭐⭐ **与 `LATCH-orchestration-boundary` 第 ② 条实证同源、互相印证:自审有效但从不自触发 ⇒ 自动 loop 里没有人提出那个要求。** <br>⭐ 现行操作规则见 `18-PROTOCOL.md` §1(R8) |
| ⭐⭐⭐ `LATCH-orchestration-boundary`<br>编排层能自动化到哪<br>(2026-08-22,Q17) | — | ⭐ **可自动化:**生成 prompt · 执行 · 汇报 · **提议**改动。<br>⛔ **不可自动化(两条):**<br>**①** 中控 AI 判定 pass/fail —— ⛔ 违反 **T1**(判定者与被判定者同源)。**实证:**`LATCH-subagent-override` —— **逐字写明的 READ-ONLY 禁令未拦住写盘,而 hook 拦住了**(`15-HOOKLAB`)⇒ **prompt 形式的判定与禁令,免疫度均为 0**<br>**②** AI 自改判据 / 自迭代约束自己的 prompt —— ⛔ **越松越容易通过 = 阻力越小**;无外部固定判据时,**没有任何力量反对放松** ⇒ ⛔ **约束单调放松(方向反了的棘轮)**;且违反「判据必须先于执行设定」 | **存在。** | ⇒ ⭐ **判定必须是 gate-check 退出码;判据变更走 amendment + 人签。**D3 分级放行已覆盖大部分设计 | ⚠️ **两条来自本项目的实证:**<br>**①** nonconstant 侧 8 次同源错误,⛔ **无一次自己发现** —— 全部由对侧 session 拦下 ⇒ ⭐ **不是能力问题,是位置问题:下达者看不见自己的盲区。**<br>**②** nonconstant 侧自审有效,⛔ **但从不自触发** —— 每次都由外部要求「用第一性原理检验」才发生 ⇒ ⭐ **自动 loop 里没有人提出那个要求。**<br>⇒ ⭐⭐ **编排层的目标不是「自动化人」,而是减少人的搬砖、保留人的判断:人从「每一步的传声筒」变为「每个 phase 的签字人」。** <br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4(C1) · §2(K6) |
| ⭐⭐ `LATCH-fix-cost-monotonicity`<br>母题:修复成本随时间上升 ⇒ 必然固化<br>(2026-08-22,**T6 候选**) | — | ⭐ 三条已记条目**共用同一骨架**:<br>· **T5** —— 失败不可观测 → 无人修 → 累积 ⇒ **必然到灾难**<br>· `LATCH-self-referential-deadlock` —— 约束阻止记录自身缺陷 → 无人修 → 累积 ⇒ **必然固化**<br>· `LATCH-budget-eats-the-fix` —— 修正占预算 → 修正越来越贵 → 越不敢修 ⇒ **必然固化** | **存在,三次。** | ⛔ 三者都不依赖失败率高低,只依赖**速率 > 0** | ⭐⭐ **母题:任何让「修复成本随时间上升」的机制,必然固化错误。**⇒ 已记 `01-PLAN.md` §6 **T6 候选**;⛔ 升格进 §1 定理表须走 amendment(且会推高现行结论行数),⛔ 本轮不做 |
| `LATCH-incomplete-set-9`<br>行数分区枚举不完整<br>(2026-08-22,A001) | — | Q15 点名「现行结论 §1/§3/§5/§7/§10 · 档案 §2/§8/§9」,⛔ **§4 缺陷台账与 §6 未决问题两节未分类**。执行方按**分类判据**(这一节是不是每次都读)而非字面枚举处置,判为现行结论 ⇒ **293**(字面枚举则为 252) | ⚠️ **`LATCH-incomplete-set` 同族第 9 次**,由执行方拦下 | ⛔ 照字面执行会少算 36 行,且下次新增章节又要重吵 | ⭐ 用户裁定 **293 对**。⇒ 已在 §7a **就地写死分区规则**:档案仅 §2/§8/§9,其余一律计入,新增章节默认计入,除非在 A00x 中显式划为档案 <br>⭐ 现行操作规则见 `18-PROTOCOL.md` §1(R5) |
| ⭐⭐ `LATCH-budget-eats-the-fix`<br>预算把修正机制本身吃掉<br>(2026-08-22,A001) | — | `CLAUDE.md` **B6 未区分「文件」与「文件类别」**。若**每份 amendment 各占 1 个名额**,八个名额会被 amendment 吃光 | **存在。**⇒ 处置:`amendments/` 作为**一个目录整体**占第 4 个名额 | ⛔ **修正契约的成本随修正次数上升 ⇒ 越到后期越不敢修 ⇒ 契约固化 —— 正是 M4 要防的** | ⚠️ **与 `LATCH-self-referential-deadlock` 同形**:两者都是**修正机制被自律型约束卡住**(一个被行数卡、一个被文件数卡)。⇒ ⭐ 一般化:**任何自律型预算,都必须把"修正自身"排除在计数之外。**与 **Q12** 同族(未区分判据与数据 / 未区分文件与类别)。⛔ 改 B6 本身另走 amendment <br>⭐ 现行操作规则见 `18-PROTOCOL.md` §3(F5) |
| ⭐⭐ `LATCH-input-control`<br>判定的**输入**被被判定者控制<br>(2026-08-22,`f50e95c`) | — | Phase 1 的闸比对 `base`→`HEAD`,而 **`base` 由调用方提供**。⇒ 被判定者可**先改判据、再把 base 设成改后状态** ⇒ 差集为空 ⇒ **判绿**。由执行方在 Phase 1 收尾时自行报出(`reports/phase1-ae6f82f.md` `known_gaps` #3) | **存在。**⚠️ 准确表述:**闸做对了,⛔ 但它的输入尚未被保护** | ⛔ 判定者独立、判据正确、退出码正确 —— 仍可被绕过,**因为绕的是输入** | ⭐⭐ **T1 补强:不只判定者要独立于被判定者,判定的输入也必须。**⇒ 强制「base 由操作者给」需要编排层 ⇒ **归 Phase 3**。已记 `01-PLAN.md` §6 **Q16** <br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4(C5) |
| `LATCH-criterion-measures-wrong-thing`<br>判据测不到它想测的东西<br>(2026-08-22,`f50e95c`) | — | §10 Phase 1 判据写作 `touch nonconstant.yml && bash .nonconstant/gates.sh` ⇒ 非 0,**实测 ⇒ `0`** —— `touch` 只改 mtime,内容判据看不见 | ⚠️ **`LATCH-constant-criterion` 同族第 3 例** | ⛔ 若迁就措辞把闸改成 mtime 判据,则 `git checkout` 只改 mtime 不改内容 ⇒ **必然误报** = `LATCH-hook-three-legs` 第三条腿 | ⭐ **执行方拒绝改判据迁就措辞,改为订正判据。⇒ 判定者独立性第七次生效。**⚠️ 一般化:判据与措辞冲突时,先问**措辞想测什么**,⛔ 不许让实现去满足字面 <br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4(C1/C2) · §1(R7) |
| ⭐⭐ `LATCH-self-referential-deadlock`<br>自律型约束阻止记录自身缺陷<br>(2026-08-22) | — | ⭐ **400 行上限正在阻止记录「400 行上限有问题」这件事** —— Q15 的完整论证写不进去,只能压成一行 | **存在。** | **推导:**约束阻止记录自身缺陷 ⇒ **不可自我修正** ⇒ **必然固化错误**(与 T5 同形:失败无输出 ⇒ 无人修 ⇒ 累积) | ⭐⭐ **任何自律型约束,必须允许「记录自身缺陷」的例外通道。**⚠️ §8「只增不删」同为自律型,**同样需要**。⛔ 例外通道的设计归 Phase 1 后的 amendment <br>⭐ 现行操作规则见 `18-PROTOCOL.md` §3(F5) |
| `LATCH-proxy-criterion`<br>只想让数字变小,没问数字在测什么<br>(2026-08-22) | — | nonconstant 侧下达「精简 §8、搬去 LEDGER」的指令,⛔ **未先问「400 这个数在测什么」**就去想办法让数字变小。⭐ 信息**一字未少**,只是换了账本 ⇒ ⛔ **不是精简,是藏账** | **存在。**⚠️ 判定者:nonconstant 侧**自审**(⛔ 非执行方拦下) | ⛔ 代用品达标而被代用的性质未改善 ⇒ **Goodhart** | ⭐ **一般化:凡「某个数超了」,先问这个数在测什么。**⚠️ 与 DevLoop 侧「凡『根因是 X』的断言,先问它能否解释量到的分布」**同族** —— 两者都是「先验判据本身,再执行判据」 <br>⭐ 现行操作规则见 `18-PROTOCOL.md` §3(F4) |
| `LATCH-constant-criterion-inverse`<br>命名判据提交后恒假<br>(2026-08-22,`418ac92`) | — | nonconstant 侧下达的 §10 Phase 3 命名判据 `ls "reports/phase$N-$(git rev-parse --short HEAD).md"` **以当前 HEAD 求值** ⇒ 报告一旦提交、HEAD 前移即失配 ⇒ **提交后恒假**。由执行方在 **Phase 0 收尾时撞破**(报告已写未提交的瞬间它为真,提交后就不再为真) | ⚠️ 与 `LATCH-constant-criterion` **同族但方向相反**:上次是恒真(`wc` 退出码恒 0),这次是恒假 | ⛔ 两者都是常量 ⇒ 都不是判据 | ⭐ **判定者独立性第六次生效。**修法(第一性):报告名 = **它所描述的那个 commit**,⛔ 非当下 HEAD ⇒ 判据改为「报告名里的 hash 在 git 历史中存在且是 HEAD 的祖先」,求值时机从「当下」变为「任何时候」。实测 绿 `0` / 红 `128` <br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4(C2) |
| `LATCH-constant-criterion`<br>下达的判据是常量,被执行方拦下<br>(2026-08-22,`e4ea6bb`) | — | nonconstant 侧下达的 Phase 0 判据 `git status --porcelain -- '*.py' '*.sh' \| wc -l ⇒ 0` **是常量** —— `wc` 的退出码恒为 0,对任何输入都返回相同结果。由执行方按 **§3 序 4(闸门可执行性)** 拦下,订正为 `[ "$(...)" -eq 0 ]`(语义不变,退出码真会变)。同一条推理下 Phase 1 红检由 `tests/` 改为 `nonconstant.yml`(nonconstant 无 `tests/` ⇒ 该路径同样是常量) | ⚠️ 与 `LATCH-incomplete-set` 同族(下达方与执行方分离) | — | ⭐ **判定者独立性在文档层面第五次生效**,且这次是**用 PLAN 自己的规则拦下达方** —— 判据来自 §3 序 4,不是执行方的临场判断 <br>⭐ 现行操作规则见 `18-PROTOCOL.md` §4(C2) |
| ⭐⭐ `LATCH-subagent-override`<br>只读禁令被 sub-agent 越过<br>(2026-08-22 15:04,`14346eb`) | prompt 形式的禁令,防的是 sub-agent 越权写盘 | **2026-08-22 15:04。**nonconstant session 派出 8 个只读核验 agent,提示词逐字写明「READ-ONLY,禁止 Write/Edit/git commit」。⛔ 其中一个 agent **写盘并提交** —— `docs/audit/17-DEVLOOP-DECISIONS.md`,commit `14346eb`,作者 `Hermes Bot`,commit message 与指定**一字不差**。开工时 `HEAD = 5a29b5d` 且该文件不存在(本轮首个 `ls` 为证)⇒ **时序排他** | **存在。**⇒ ⭐ **prompt 形式的禁令对 sub-agent 免疫度为 0。**推导:禁令一旦进入上下文即成为**文本**,可被权衡、解释、覆盖(T1 / T4) | ⛔ 被判定者在"禁止"状态下完成了一次完整的写 + 提交,**无任何机制阻止**。⚠️ 本次产物经抽验 18/18 引用属实,故未回滚 | ⭐⭐ **与 15-HOOKLAB 构成同场景 A/B 对照** —— 同一个动作(sub-agent 写盘):prompt 禁令 ⇒ ⛔ **未拦住**(本次);PreToolUse hook ⇒ ⭐ **拦住了**(15 号,`calls.log` L19-20 `tool=Write` + `DENIED`)。⇒ **这是 nonconstant 全部主张里最干净的一条实证**,因为它是**同一场景下的对照**,⛔ 不是两个不同事故的类比。已联动 `01-PLAN.md` §8 #17 / §6 Q11 <br>⭐ 现行操作规则见 `18-PROTOCOL.md` §1(R1~R3) |
| `LATCH-count-from-memory`<br>导读指令的文件计数<br>(2026-08-22,`413e339` 之后,事实留存) | — | nonconstant 侧下达导读指令时称"**13 份文件**";⛔ 实测 `docs/audit/` 只有 **5 份**。另有 5 份(05/07/09/11/13)从未放入仓库,3 份(04/06/08)从未提交。由执行方查实并**拒绝照字面执行** | ⚠️ 与 `LATCH-incomplete-set` 同源,⛔ **且更严重**:上次是集合不完整,这次是**按对话编号数数,而非按磁盘数数** | ⭐ 一般化:**凡"有多少个 X"的断言,必须以磁盘 / 命令输出为准,⛔ 不得以记忆或对话历史为准** | 事实留存 <br>⭐ 现行操作规则见 `18-PROTOCOL.md` §3(F1/F2) |
| `LATCH-unexecutable-constraint`<br>追加核验任务 C 的「临时目录」铁律<br>(2026-08-22,`dca84db`,事实留存) | — | nonconstant 侧下达追加核验时,铁律要求「任务 C 的实测**在临时目录里做**」。⛔ 但 `isolation: "worktree"` 的作用域**不由调用方指定** —— 工位按**会话所在的仓**建。执行方在 `%TEMP%` 下备好的靶场**根本没被用上**,两个探针落进 `C:/pg/_infra/devloop/.claude/worktrees/`。⇒ 该约束在当前工具能力下**不可执行**:执行方无论怎么做都会违反它。由执行方在实测中撞破,并**主动如实报告**(14 号报告 §C.0),⛔ 而非事后掩饰或省略。⚠️ **同一份指令里还有第二处同源**:称 249 条残骸为「2026-08-21 事故残骸」,⛔ 实测 mtime 窗口为 **2026-08-21 23:56 → 08-22 02:53**,其中 **245 条落在 08-22** | ⚠️ 与 `LATCH-incomplete-set` / `LATCH-count-from-memory` **同源,且这是第三、第四次**。⛔ 底层是同一件事:**下达方对执行环境的假设未经实测** —— 集合没数(#1)、数字没按磁盘数(#2)、**工具能力没试**(本条)、**日期没量**(本条附注) | ⭐ 一般化:**凡约束涉及工具的实际行为,下达前须有一次实测,或在约束旁明确标注「未验证」。**⛔ 违反此条时,执行方撞破约束应记为**发现**,⛔ 不记为过失 —— 否则下一个执行方会选择掩饰,而掩饰**不产生任何可观测输出**(⇒ 正是 T5 的形状) | 事实留存 <br>⭐ 现行操作规则见 `18-PROTOCOL.md` §1(R5) |
| `SK-normalize-priority`<br>`extensions/__init__.py:185-204`<br>(R1,事实留存) | — | `workflows → extensions` 的**全部依赖面 = 单个函数** `normalize_priority`(`extensions/__init__.py:185-204`),闭包仅含常量 `DEFAULT_HOOK_PRIORITY = 10`(`:73`),函数体除 `isinstance` / `int` 两个 builtin 外无外部调用。两个引用点:`overlays/schema.py:9`(在 `engine.py:886` 的 by-ID 解析路径上)、`overlays/_commands.py:13`(仅 CLI 延迟导入)。Challenger 独立复现 | — | — | **无决策价值**(删除计划已于 §8 #9 撤回,全部 spec-kit 代码 KEEP)。仅作事实留存:8,020 行里被 `workflows/` 依赖的是这 20 行 |

---

## 撤回记录

> 与 `01-PLAN.md` §8 联动。任何轮次推翻了先前主张,两处都要记。

| 轮次 | 撤回了什么 | 因何撤回 |
|------|-----------|---------|
| 0(seed 复核) | §2.2「`workflows/ → extensions/presets` 无引用,✅ 可安全删除后者」 | 对 `vendor/spec-kit`(v0.16.4/`d1f50fc`)独立复核:方向反了。`workflows/overlays/schema.py:9`、`overlays/_commands.py:13` 均 `from ...extensions import normalize_priority`(定义于 `extensions/__init__.py:185`);且 `engine.py:886 from .overlays import WorkflowResolver` → `overlays/__init__.py:15` → `schema.py:9`,即**引擎解析路径本身依赖 `extensions/`**。反向 `grep -rnE "^\s*(from\|import)\s+.*workflow" src/specify_cli/{extensions,presets}/` 退出码 1,零 import。→ 已记 01-PLAN §8 #7 |
| 0(seed 复核) | §2.2「`workflows/` ≈ 6,500 行」 | 实测 `src/specify_cli/workflows` 24 个 `.py` 共 **11,691** 行,低报 5,191(44%),远出 ±15%。同口径实测 `extensions/` 8,020(声称 8,000)、`presets/` 6,761(声称 6,800),差 <1%,证明口径就是"原始行数、含 `_commands.py`"。另试非空行 10,347、去注释 9,025,均进不了带;唯一能凑进带的"排除 `_commands.py`"(7,377)会把 extensions 打成 5,316,口径自相矛盾。→ 已记 01-PLAN §8 #8 |
