# Phase 5 · waiver 到期判据 —— 完成报告

> ⭐ **第一读者是下一个会话的模型,不是人。**
> ⚠️ 本报告绑定 commit **`30005dc`** —— ⛔ 脱离该 commit 的任何结论不可证伪。

## pin

| 项 | 值 |
|---|---|
| 报告绑定 commit | **`30005dc`** `fix: set self-scan waiver until to Phase 9` |
| 判据首次锁定于 | `57bdf83` `feat: lock Phase 5 waiver-expiry criterion` |
| 上游 pin | `github/spec-kit` **v1.0.0** / `bca6790` / MIT |
| 阶段 | **Phase 5 · waiver 到期判据** |
| 日期 | 2026-08-22 |

⚠️ ⛔ **为何绑 `30005dc` 而非 `57bdf83`**:判据在 `57bdf83` 上**判红**(自扫豁免的 `until` 不是具体 phase)。⭐ 报告须绑「**这个确定状态通过了检验**」的那个 commit(A3)。

## frozen_contracts

| ID | 契约 | 落点 |
|---|---|---|
| P5-C1 | ⭐ **豁免的 `until` 必须是「Phase &lt;n&gt;」** —— ⛔ 没有 `Phase <n>` 的措辞等于「以后」 | `.latch/waiver-expiry.sh` |
| P5-C2 | ⭐ **`n` 须 > 当前 phase** —— 当前 phase 取自 `reports/phase<N>-*.md` 最大编号 | 同上 |
| P5-C3 | ⭐⭐ **`n` 须 ≤ §10 已排的最大 phase** —— ⛔ 指向未排期的 phase = 「以后」换个写法(空头支票) | 同上,`### Phase <n>` 解析自 `01-PLAN.md` |
| P5-C4 | ⭐ **零豁免 ⇒ 判 `0`** —— 本判据断言「**每一条**豁免都未过期」,零条时为真 | 同上 |

## references_contracts

| 上游契约 | 出处 | 兑现 |
|---|---|---|
| §10 Phase 5 | `01-PLAN.md` | ⭐ 两条验收 + 五类探针全做 |
| C8 写下的上限必须有机器判据 | `18-PROTOCOL.md` §4 | ⭐ 本判据是它在**到期**维度的兑现:写下的到期没有判据 = 没有到期 |
| C2 一过一失败 · C3 红检对 fixture 跑 | `18-PROTOCOL.md` §4 | ⭐ fixture 由 `cp latch.yml` 动态复制后变异,`$TEMP` 内,⛔ 仓库零静态副本 |
| C6 闸门缺失比判红更严重 | `18-PROTOCOL.md` §4 | ⭐ 配置 / PLAN / `reports/` 缺失均判 **2** |
| F6 未量根因的成本估算不得作为放弃依据 | `18-PROTOCOL.md` §3 | ⭐ 本轮正是它的第一次应用 —— 见 `known_gaps` #2 |
| P4-C4(P3-C1 推论)演示在先、登记在后 | `reports/phase4-900b8de.md` | ⭐ 脚本 `57bdf83` 锁定 → 本报告 → 才登记进 `latch.yml` |

## gate_results

⭐ 全部在 **HEAD = `30005dc`** 上求值;每次探针后复原,收尾工作树 **0 项**。

| # | 检 / 探针 | 期望 | 实测 |
|---|---|---|---|
| 1 | **绿检** —— 真源(`until: Phase 9`) | 0 | ⭐ **0** |
| 2 | **红检 1** —— `until: Phase 1`(已完成) | 非 0 | ⭐ **1**「已过期:until = Phase 1,而当前已到 Phase 4」 |
| 3 | **红检 2** —— `until: 以后再说` | 非 0 | ⭐ **1**「没有『Phase &lt;n&gt;』⇒ 等于「以后」」 |
| 4 | ⭐ **红检 3** —— `until: Phase 99`(未排期) | 非 0 | ⭐ **1**「Phase 99 尚未排期(§10 只排到 Phase 9)」 |
| 5 | 探针 B · 一条豁免都没有 | 判断见下 | ⭐ **0** |
| 6 | 探针 C1 · 配置不存在 | 非 0 | ⭐ **2** |
| 7 | 探针 C2 · `01-PLAN.md` 不存在 | 非 0 | ⭐ **2** |
| 8 | 探针 D · `reports/` 为空(算不出当前 phase) | 非 0 | ⭐ **2** ⛔ 不当成「没有过期 ⇒ 放行」 |
| 9 | 探针 E · 判据自身被删(**已提交版本**) | 非 0 | ⚠️ **127**,⭐ 且 `criteria-guard` 判 **1** |

⇒ ⭐ **一过一失败已演示(#1 vs #2/#3/#4),三类判红各有独立红检 ⇒ 不是常量。**

### ⭐ 探针 B 判 `0` 的理由(P5-C4)

⚠️ 与 `status-facts` 的**空 STATUS 判 1** 是**不同**的判断,⛔ 不矛盾:
- `status-facts`:STATUS 的**全部职责**就是断言状态 ⇒ 零断言 = **未履行职责** ⇒ 判 1
- ⭐ 本判据:断言的是「**每一条**豁免都未过期」⇒ 零豁免 = **本来就没欠债** ⇒ 判 0

### ⭐ 探针 E 的补充发现

⚠️ 首次跑探针 E 时 `criteria-guard` 判 **0**(没看见删除)—— 因为该文件当时**尚未提交**。
⇒ ⛔ **criteria-guard 只能看见「已进入版本控制的东西」的变化**;一个从未提交的判据对它而言**不存在**。
⇒ 已记 `03-LEDGER.md` `LATCH-untracked-invisible`,⛔ 本 phase 未修(改 `gates.sh` 需授权)。

## known_gaps

| # | 缺口 | 说明 |
|---|---|---|
| 1 | ⚠️ **动用了预算外余量,登记如下**(A003 要求) | **用了什么:**`.latch/waiver-expiry.sh`(84 行)。**属哪类:**⭐ **①「修正自身的产物」** —— 它检查的是 latch 自己的豁免账期,是**修正机制本身**。**为什么名额装不下:**B6 八格已**全部指名**,第 8 格 `workflows/latch/workflow.yml` 留给 Phase 7/8;挤掉它只是把问题换位置(A003 §3 已否决该选项) |
| 2 | ⭐⭐ **本 phase 的死结由一次「证伪」解开,⛔ 不是由判据解开** | 自扫豁免的 `until` 原为「引入真解析器后移除」——⛔ 一个**没量根因**的成本估算(「上千行」⇄ 实际 **0 净增**)。实测后才排出 **Phase 9**,`until` 才有可指的 phase。⇒ ⚠️ **判据只告诉你「这条到期不合格」,⛔ 它不会告诉你「原因估错了」** —— 那一步仍靠人。见 **F6** |
| 3 | ⛔ **过期只判红,无强制清理** | P4/P6 设想的「过期未清 ⇒ hard 阻断」只做到「判红」。⚠️ 若无人跑这条判据,过期照样静默 —— ⇒ 归编排层(**Q17**) |
| 4 | ⚠️ **判据现在依赖 §10 的内容**(P5-C3 的代价) | `### Phase <n>` 标题一变,判据行为就变。⭐ 这是**期望的**(排了新 phase,豁免才能指向它),⛔ 但将来有人改 §10 会不知道踩到判据。⇒ 必须记在此处 |
| 5 | ⚠️ **只认 `until:` 与 `waiver_until:` 两个键名** | 将来新增别的豁免字段名 ⇒ ⛔ **静默漏检**(T5 形状) |
| 6 | ⚠️ **`until` 的措辞不受判据约束** | P5-C1 只要求含 `Phase <n>`;⛔ 「解除条件」写得对不对写不成退出码(D5)。⇒ 本轮靠人写清:`Phase 9 —— 自扫豁免被【消除】(⛔ 非被满足)` |
| 7 | ⚠️ **删判据仍返回 127** | 同源第 5 次。⭐ `criteria-guard` 兜住(**已提交**时判 1),⛔ 编排层仍须显式处理(C6) |

## next_entry_conditions

Phase 6(上游语义判据)开工前须满足:

1. ⭐ 本报告已提交,`.latch/waiver-expiry.sh` 在 `30005dc` 内 —— **已满足**
2. ⬜ 红/绿两条命令**先写出来**再写实现
3. ⚠️ Phase 6 判**语义不判文件内容** —— ⛔ 给 `vendor/` 做指纹会因**合法升级**误报(= `LATCH-hook-three-legs` 第三条腿)
4. ⚠️ 两条语义的出处已由 **Q14** 实测:`workflows/steps/shell/__init__.py`(退出码非 0 ⇒ 停)· `workflows/steps/gate/__init__.py`(`on_reject` 默认 `abort`)
5. ⛔ **`vendor/` 在 `.gitignore` 内、未跟踪** ⇒ ⚠️ 判据须能在**未跟踪的目录**上工作(与 `LATCH-untracked-invisible` 同一根因的另一面)

## explicitly_out_of_scope

⛔ 下一会话**不得重提**:

- ⛔ 实施候选 1(改 `scan-silent` 消息文本)—— 那是 **Phase 9**,⛔ 不是本 phase
- ⛔ 把 `until` 改回不含 `Phase <n>` 的措辞
- ⛔ 允许 `until` 指向未排期的 phase(P5-C3 已判定 = 空头支票)
- ⛔ 让零豁免判红(P5-C4 已判定)
- ⛔ 修 `criteria-guard` 的未跟踪盲区(已记 `LATCH-untracked-invisible`,须授权)
- ⛔ 建编排层 / 定安装形态(Phase 7 / 8+)

## human_confirmation

⛔ **模型不得代填。**

- [ ] 我确认 `until: Phase 9 —— 自扫豁免被【消除】(⛔ 非被满足)` 这个措辞
- [ ] 我确认「零豁免判 0」与「空 STATUS 判 1」两个不同判断都成立
- [ ] 我确认 `waiver-expiry.sh` 动用预算外余量的登记(`known_gaps` #1)
- [ ] 我确认 `known_gaps` #4(判据依赖 §10 内容)这个耦合可以接受
- [ ] 我确认 Phase 5 可以关闭
